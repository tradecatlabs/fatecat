# FateCat 测算基础设施 100% 实现计划

> 状态：Living Plan
> 最近刷新：2026-07-03
> 用途：把 FateCat 从“测算工具/报告服务”推进为面向 Agent 与应用开发者的测算基础设施。
> 边界：本文是实现计划和验收口径，不等于全部已完成；真实生产 token、真实 Bot、外部 SIEM、外部监控、OIDC/IdP 和第三方审计仍需单独执行。0050 已完成一次 GHCR digest 与 GitHub artifact attestation，但后续每个 release commit 仍必须重新执行远端 container workflow。

## 0. 2026-07-02 Post-0046 计划刷新

本次刷新基于 `governance/tasks/0009` 到 `0047`、`contracts/fate/delivery/release-gate.json`、当前工作树事实、远端 GitHub Actions 状态和外部基础设施一手资料。结论是：FateCat 距离“100% 测算基础设施”已经不是缺少一个大模块，而是缺少最后一组不可伪造的生产闭环：真实 Bot live、registry digest/signature/attestation、生产身份/监控/SIEM 证据、长期运行控制面、外部审计包，以及八字/紫微核心能力的大规模 golden corpus。

### 0.1 外部基础设施同构调研

| 成熟领域 | 官方依据 | 这些系统真正卖的能力 | FateCat 100% 必须具备的对应能力 |
| --- | --- | --- | --- |
| API 平台 | [OpenAPI Specification](https://spec.openapis.org/oas/latest.html) | 机器可读接口、schema、错误、示例、版本 | OpenAPI、标准错误码、SDK 示例、API changelog、兼容策略 |
| 支付基础设施 | [Stripe Idempotent requests](https://docs.stripe.com/api/idempotent_requests)、[Stripe Webhooks](https://docs.stripe.com/webhooks) | 幂等、防重复副作用、异步事件交付 | report job 幂等、callback 签名、retry/timeout、事件历史 |
| 持久工作流 | [Temporal Durable Execution](https://docs.temporal.io/evaluate/understanding-temporal)、[Temporal Retry Policies](https://docs.temporal.io/encyclopedia/retry-policies) | 长流程可恢复、重试声明化、状态可靠 | durable job store、restart recovery、provider retry policy、非重试错误分类 |
| 控制面 | [Kubernetes Controllers](https://kubernetes.io/docs/concepts/architecture/controller/) | desired/current state、reconciliation、ready 状态 | capability admission、provider lifecycle、release gate、policy drift scanner |
| Provider 生态 | [Terraform Providers](https://developer.hashicorp.com/terraform/language/providers) | 第三方能力插件化、版本化、锁定化 | bazi/ziwei/almanac/meihua provider metadata、version lock、license/source gate |
| 软件目录 | [Backstage System Model](https://backstage.io/docs/features/software-catalog/system-model/) | Component/API/Resource/System 可发现 | Capability、Provider、Dataset、EvaluationRun、SecurityControl、DeliverySurface catalog |
| 可观测 | [OpenTelemetry Signals](https://opentelemetry.io/docs/concepts/signals/) | traces、metrics、logs 三信号 | API -> job -> provider -> report trace、metrics、结构化日志、requestId/traceId |
| SRE | [Google SRE SLO](https://sre.google/sre-book/service-level-objectives/) | 用 SLO/error budget 管可靠性 | p95/p99、错误率、job success、provider failure、error budget burn alert |
| 交付效能 | [DORA metrics](https://dora.dev/guides/dora-metrics-four-keys/) | 部署频率、变更前置时间、失败率、恢复时间 | release gate、rollback drill、CI evidence、change failure 和 MTTR 记录 |
| 供应链安全 | [SLSA v1.2](https://slsa.dev/spec/v1.2/)、[CycloneDX](https://cyclonedx.org/specification/overview/) | provenance、attestation、SBOM | release artifacts、container digest、registry signature、source/vendor/data manifest |
| API 安全 | [OWASP API Security Top 10 2023](https://owasp.org/API-Security/editions/2023/en/0x11-t10/) | BOLA、认证、资源消耗、配置、库存风险 | RBAC、token、rate limit、API inventory、privacy regression、negative tests |
| 安全开发 | [NIST SSDF SP 800-218](https://csrc.nist.gov/pubs/sp/800/218/final) | 安全软件开发、供应链和漏洞响应 | secure defaults、secret scan、dependency review、release evidence、incident response |
| 事件格式 | [CloudEvents](https://cloudevents.io/) | 事件元数据标准化 | webhook event envelope、job terminal events、callback audit |

### 0.2 100% 的真实定义

FateCat 达到 100% 时，必须同时满足：

- 对开发者：不读源码也能通过 OpenAPI、SDK 示例、sandbox、错误码和文档接入。
- 对 Agent：所有体系必须通过 capability registry 和 executor 调用，不能让模型自行重算或拼报告。
- 对使用者：报告结构稳定、免责声明稳定、隐私边界稳定、证据可追溯。
- 对审计者：数据、典籍、vendor、规则、测试、CI、发布证据可以逐项复核。
- 对运维者：health/ready/metrics/traces/logs/SLO/alerts/runbook/rollback 可用。
- 对发布者：local CI、远端 CI、container、SBOM、provenance、live smoke、rollback、clean git 全部可证明。

### 0.3 Post-0046 当前最短剩余路径

| 顺序 | 后续任务 | 目标 | 必须证据 |
| --- | --- | --- | --- |
| Done | release clean state 与远端 CI baseline | 0046 已把改动提交推送到 `main`，Container 与 Acceptance workflow 均对 `2b3f4c8` 通过 | `git status` clean、commit `2b3f4c8`、Container run `28575853017` success、Acceptance run `28575852876` success |
| MI-NEXT-01 | Telegram Bot live smoke | 0048 已执行 live smoke 前置检查；当前缺少真实 `FATE_BOT_TOKEN`，保持 Blocked | `scripts/live-bot-smoke.sh` 使用真实 `FATE_BOT_TOKEN` 通过；不输出 token；当前失败证据为缺 token |
| Done | registry digest 与签名/attestation | 0050 已把本地 imageId baseline 提升为 GHCR digest、GitHub artifact attestation 和 release artifact CI upload | Container workflow `28580109478` success、digest `sha256:2544ae3c30ce66141652199dbf065cd168ec2618c7b51bb6afc922172d9a0756`、attestation `33588466` |
| MI-NEXT-03 | durable runtime 二期 | 从本地 SQLite baseline 升级到可恢复任务控制面；0052-0060 已落地 job event history、本地 retry/timeout、callback retry/outbox、restart recovery、SQLite outbox/config/lease baseline，0070 已落地 Postgres adapter，0071 已落地 Postgres migration/job live smoke baseline，0072 已落地 Postgres outbox worker lease negative smoke，0074 已落地 Postgres job execution worker lease primitive smoke，0075 已落地 Postgres external worker restart smoke，0076 已落地 Postgres public webhook live smoke gate，0078 已落地 Postgres worker heartbeat/polling smoke baseline，0079 已落地外部 secret provider/Vault/KMS evidence gate baseline，0080 已落地长期多副本 runtime evidence gate baseline，0081 已落地长期多副本 runtime evidence assembler baseline | 真实公网 webhook live passed evidence、外部 Vault/KMS live passed、生产密钥生命周期、长期多副本运行 live evidence 和 exactly-once 仍待后续 |
| MI-NEXT-04 | OpenTelemetry collector 与生产 SLO | 从本地 trace/log baseline 升级到外部观测平台 | collector/exporter 配置、trace backend 链接、SLO/error budget evidence、alert dry/live evidence |
| MI-NEXT-05 | OIDC/SIEM/retention 实测 | 从安全 contract 升级到真实平台接入 | OIDC/IdP evidence、SIEM evidence、retention cleaner smoke、OWASP/RBAC regression |
| MI-NEXT-06 | 八字/紫微 golden corpus 扩容 | 把 L4 样板从代表样本推进到专业回归集 | 匿名 corpus、边界覆盖报告、全文 report diff、冲突裁决解释 |
| MI-NEXT-07 | MingLi-Bench 与外部 benchmark runner | 把外部评测资产纳入可选 gate | runner summary、license/usage 分级、diff policy、nightly artifact |
| MI-NEXT-08 | provider trace 与 lifecycle drift scanner | 防止 provider 状态、依赖、license 和实际执行漂移 | lifecycle gate、dependency smoke、trace span、drift report |
| MI-NEXT-09 | developer portal / SDK 发布 baseline | 从本地示例升级到外部开发者体验 | 发布版 OpenAPI、SDK package 或 installable examples、sandbox token、固定输出 snapshot |
| MI-NEXT-10 | 第三方审计包 | 把所有证据打包成可交接审计材料 | audit handoff markdown/json、证据路径、未验证项、风险登记 |

### 0.4 当前不允许宣称完成的项

- Telegram Bot live smoke：0048 已执行到阻断点；外部连通验证待执行，需要真实 `FATE_BOT_TOKEN`。
- 真实 registry digest/signature：0050 已完成 GHCR digest 与 GitHub artifact attestation；后续每个 release commit 仍需重新跑 container workflow。
- 真实 OIDC、SIEM、监控平台、告警平台：当前只有本地 contract/gate baseline。
- 典籍和外部资料的法律意见：当前有机器可读分级，不等于法律审计完成。

## 0.5 2026-07-02 深度调研补强：100% 基础设施定义

本轮补强基于当前 `main` worktree、0048 Telegram Bot live 阻断事实，以及基础设施领域一手资料重新定义“100%”。这里的 100% 不是预测命中率，也不是功能模块数量，而是基础设施成熟度：能力可发现、接口可接入、运行可恢复、证据可审计、质量可回归、发布可证明、事故可处置。

### 0.5.1 同构资料与 FateCat 映射

| 外部基础设施范式 | 一手资料 | 提炼出的基础设施能力 | FateCat 对应实现要求 |
| --- | --- | --- | --- |
| 平台工程成熟度 | CNCF Platform Engineering Maturity Model: https://tag-app-delivery.cncf.io/whitepapers/platform-eng-maturity-model/ | 平台作为产品，围绕用户自助、能力治理、反馈、度量和持续改进提升成熟度 | FateCat 不能只堆测算模块；必须把 capability、provider、job、report、audit、developer experience 做成可发现、可自助、可度量、可演进的平台能力 |
| 云原生系统 | CNCF Cloud Native Definition: https://www.cncf.io/about/who-we-are/ | 松耦合、弹性、可管理、可观测、自动化 | capability/provider/report/job 必须解耦；所有发布、验证和回滚通过脚本/CI 自动化 |
| HTTP API 契约 | OpenAPI 3.2: https://spec.openapis.org/oas/latest.html | 不读源码即可理解接口能力 | `/api/v1/*` 必须有 OpenAPI、错误码、示例、版本和兼容策略 |
| 异步 API 契约 | AsyncAPI 3.1: https://www.asyncapi.com/docs/reference/specification/latest | 消息驱动接口机器可读 | report job terminal event、webhook callback、evaluation event 应形成 AsyncAPI/CloudEvents 契约 |
| 事件信封 | CloudEvents: https://cloudevents.io/ | 事件元数据统一，便于路由、审计、追踪 | webhook/job/evaluation/release 事件统一 `id/source/type/subject/time/data` |
| 控制面 | Kubernetes controller pattern: https://kubernetes.io/docs/concepts/architecture/controller/ | spec/status、desired/current reconciliation | capability/provider/release gate 要有 desired spec、runtime status、drift scanner |
| 软件目录 | Backstage system model: https://backstage.io/docs/features/software-catalog/system-model/ | Component/API/Resource 可发现 | Capability、Provider、Dataset、EvaluationRun、DeliverySurface、SecurityControl 全部进入 catalog/registry |
| Provider 生态 | Terraform providers: https://developer.hashicorp.com/terraform/language/providers | provider 独立版本、锁定、配置和安装 | bazi/ziwei/almanac/meihua provider 必须锁版本、声明来源、license、health、dependency |
| 持久工作流 | Temporal durable execution: https://docs.temporal.io/evaluate/understanding-temporal | 长流程状态持久、失败后恢复、event history | report/evaluation job 需要 event history、retry/timeout、restart recovery、external backend |
| 可观测 | OpenTelemetry signals: https://opentelemetry.io/docs/concepts/signals/ | traces/metrics/logs/baggage 统一 | API -> job -> provider -> report 链路要可 trace，指标和日志必须能关联 requestId/traceId |
| SRE 可靠性 | Google SRE SLO: https://sre.google/sre-book/service-level-objectives/ | 用 SLI/SLO 管理服务健康 | availability、latency、job success、provider success、error budget、alert/runbook |
| API 安全 | OWASP API Security Top 10 2023: https://owasp.org/API-Security/editions/2023/en/0x11-t10/ | BOLA、认证、资源消耗、函数授权、库存风险 | token/RBAC/rate limit/body limit/API inventory/negative tests 必须覆盖 |
| 安全开发 | NIST SSDF SP 800-218: https://csrc.nist.gov/pubs/sp/800/218/final | 安全开发、漏洞减少、供应链沟通 | secret scan、依赖审计、release evidence、漏洞响应和审计包 |
| AI 风险治理 | NIST AI RMF: https://www.nist.gov/itl/ai-risk-management-framework | AI 产品设计、使用、评估中的风险管理 | AI/Agent 只能解释结构化结果；必须禁止确定性断语和高风险替代建议 |
| 供应链等级 | SLSA v1.2: https://slsa.dev/spec/v1.2/ | provenance、attestation、逐级供应链保证 | GitHub Actions attestation、registry digest、SBOM、source/vendor/data provenance |
| SBOM | CycloneDX: https://cyclonedx.org/specification/overview/ | 组件、服务、依赖、关系和 license 可机器读取 | release SBOM、data/vendor SBOM、service dependency graph 和 export policy |
| GitHub 发布证明 | GitHub artifact attestations: https://docs.github.com/en/actions/how-tos/secure-your-work/use-artifact-attestations/use-artifact-attestations | build provenance、SBOM attestation、CLI verify | `.github/workflows/container.yml` 需要 digest 输出、artifact attestation 和 `gh attestation verify oci://...` |

### 0.5.2 100% 基础设施资源模型

FateCat 的终态资源不应再以“页面/脚本/报告”组织，而应以资源对象组织：

| Resource | 作用 | 当前状态 | 100% 缺口 |
| --- | --- | --- | --- |
| `Capability` | 测算能力的唯一执行入口 | 已有 registry 和 executor | promotion/drift scanner、AsyncAPI event link、完整 status |
| `Provider` | 成熟库/外部算法/自有适配层 | 已有 provider protocol | 真实依赖 health、version lock 自动校验、trace span、deprecation gate |
| `CalculationJob` | 同步/异步测算任务 | memory/sqlite baseline；0052-0060 已补 job event history、本地 retry/timeout、callback retry/outbox、restart recovery、SQLite outbox/config/lease baseline；0070 已补 Postgres adapter；0071 已补 Postgres migration/job live smoke baseline；0072 已补 Postgres outbox worker lease negative smoke；0074 已补 Postgres job execution worker lease primitive smoke；0075 已补 Postgres external worker restart smoke；0076 已补 Postgres public webhook live smoke gate；0078 已补 Postgres worker heartbeat/polling smoke baseline；0079 已补外部 secret provider evidence gate；0080 已补长期多副本 runtime evidence gate | 真实公网 webhook live passed evidence、外部 Vault/KMS live passed、生产密钥生命周期、长期多副本运行 live evidence 和 exactly-once |
| `ReportProfile` | Markdown/JSON 输出结构 | bazi/ziwei/almanac/meihua baseline | 全文 golden diff、跨端同源 diff、人审导出 |
| `Evidence` | 结论依据、规则、权重、风险 | 已有 evidenceRefs/policy gate | 覆盖率报告、断链 fail-fast、反证说明 |
| `Dataset` | golden、benchmark、典籍、节气表 | 已有 data supply chain registry | 大规模匿名 corpus、法律/版权人工意见、数据版本晋级 |
| `EvaluationRun` | 质量评测与回归 | runner/history/dashboard baseline | nightly 当前 commit artifact、长期趋势库、外部 benchmark runner |
| `DeliverySurface` | Web/API/Bot/CLI/Skill/HF | 已有 registry | 真实 live diff、Bot live、SDK/package、developer portal |
| `SecurityControl` | 认证、授权、隐私、审计 | 本地 smoke/contract baseline | OIDC/IdP、外部 SIEM、retention cleaner、租户隔离 |
| `ObservabilitySignal` | health/ready/metrics/logs/traces/SLO | 本地 OTel-compatible baseline | OTel SDK/collector/backend、真实 error budget、alert live |
| `ReleaseArtifact` | 容器、SBOM、provenance、rollback | 本地 baseline | GHCR digest、attestation/signature verify、current commit CI |
| `KnowledgeSource` | 典籍、规则、外部仓库 | 已有 manifest 和 canonical TXT | 权威性分级、人审摘录、引用层自动化 |
| `Policy` | 风险、免责声明、禁止断语 | 已有 risk policy | 全入口 policy enforcement、LLM 输出扫描、违规回归 |
| `AuditHandoff` | 第三方审计交接包 | 多任务 closeout 分散存在 | 一键聚合审计包、证据链接、未知项清单 |

### 0.5.3 100% 实现波次

| Wave | 目标 | 产物 | 关键验收 |
| --- | --- | --- | --- |
| W0 Release proof | 清掉不可伪造发布证据缺口 | GHCR digest、GitHub attestation、SBOM attestation、current commit CI、rollback evidence | `gh attestation verify oci://...`、release gate `passed=all`，Telegram live 仍需 token |
| W1 Control plane | 把 capability/provider/report/job 都变成 spec/status 资源 | registry status、drift scanner、promotion checklist、negative tests | planned 拒绝执行；status 漂移 fail-fast |
| W2 Developer platform | 让外部开发者不读源码即可接入 | OpenAPI 3.2、AsyncAPI、SDK examples/package、sandbox token、fixed snapshots | docs smoke、SDK smoke、API changelog |
| W3 Durable runtime | 让测算任务可恢复、可取消、可回放 | external job store、event history、retry/timeout、callback retry | restart recovery smoke、幂等和重试 negative tests |
| W4 Provider platform | 让每个体系像 provider 一样治理 | provider lock、health、trace、license/source gate、deprecation | provider lifecycle gate + dependency smoke + trace smoke |
| W5 Evidence and eval | 让命理结论可回归、可审计 | bazi/ziwei corpus、MingLi-Bench runner、evidence coverage、conflict explainer | golden diff、benchmark summary、evidenceRefs 0 断链 |
| W6 SRE operations | 让生产问题可观察、可响应 | OTel collector、dashboard、SLO/error budget、alerts、runbook | trace backend URL、alert dry/live evidence、incident drill |
| W7 Security/privacy | 让公开服务默认安全 | OIDC/IdP、RBAC negative tests、SIEM、retention cleaner、privacy regression | OWASP API pack pass、SIEM evidence、retention smoke |
| W8 Multi-surface parity | 让 Web/API/Bot/CLI/Skill 同源 | semantic diff tool、Bot live、HF/API live、Markdown copy snapshot | 多端 diff pass，Bot live pass |
| W9 Audit package | 让第三方可复核 | audit handoff markdown/json、evidence bundle、risk register closeout | 审计包无伪证、未验证项全标记 |

### 0.5.4 立即执行顺序

当前 0048 因缺真实 `FATE_BOT_TOKEN` 阻断，不能等待外部 secret 阻塞全局推进。0050 已完成 `MI-NEXT-02` GHCR registry digest、GitHub artifact attestation 与 verify gate。下一步按以下顺序执行可独立完成的任务：

1. `MI-NEXT-03`：durable runtime 二期，继续补真实公网 webhook live passed evidence、外部 Vault/KMS、生产密钥生命周期、heartbeat/polling worker、长期多副本运行和 exactly-once 边界。
2. `MI-NEXT-04`：OpenTelemetry SDK/collector、trace backend、SLO/error budget、alert evidence。
3. `MI-NEXT-05`：OIDC/IdP、SIEM、retention cleaner 和 OWASP API regression 实测。
4. `MI-NEXT-06`：八字/紫微 golden corpus 扩容、MingLi-Bench runner 和证据覆盖率。
5. `MI-NEXT-09`：developer portal、SDK 发布 baseline、sandbox token 和固定输出 snapshot。
6. `MI-NEXT-10`：聚合第三方审计包。

### 0.5.5 完成判定

测算基础设施 100% 只能在以下全部成立后声明：

- 所有 production capability 均通过同一个 executor、provider protocol、policy gate、evidence gate 和 report profile。
- 所有外部 delivery surface 均有 live smoke；Telegram Bot live 不能用 dry-run 替代。
- 所有 release artifact 均有 registry digest、SBOM/provenance、attestation/signature 和 verification command。
- 所有 SLO/alert/SIEM/OIDC 结论均来自真实外部平台证据或明确标记 `外部连通验证待执行`。
- 八字/紫微核心 corpus 和 benchmark 能覆盖历法边界、格局边界、紫微典型格局、报告输出和冲突解释。
- 第三方审计包可以从 Git 状态、CI、artifact、contract、registry、script 输出逐项复核。

## 0.6 2026-07-02 Post-0050 可执行实现计划

本轮补强基于当前 `0050` registry attestation 结果、0048 Bot live blocker、现有 `contracts/fate/*` 资源，以及基础设施同构资料继续收敛。核心判断：FateCat 要成为 100% 测算基础设施，后续不能再按“先做哪个术数功能”推进，而要按 infra 控制面推进。每个能力必须经历：契约 -> provider -> job/runtime -> evidence/report -> eval -> observability -> security -> release -> audit。

### 0.6.1 当前完成面与缺口

| 域 | 当前完成面 | 100% 剩余缺口 | 下一任务 |
| --- | --- | --- | --- |
| Release proof | 0050 已完成 GHCR digest、release artifact upload、GitHub artifact attestation、verify gate | 每个 release commit 重跑；SBOM attestation 仍可增强；release evidence 需聚合到审计包 | MI-NEXT-10 |
| Bot live | 0048 已证明脚本和前置逻辑可执行 | 缺真实 `FATE_BOT_TOKEN`，外部连通验证待执行 | MI-NEXT-01 |
| Durable runtime | memory/sqlite job store、idempotency、cancel、webhook baseline、0052-0060 SQLite/job/webhook recovery baseline、0070 Postgres adapter、0071 Postgres migration/job live smoke baseline、0072 Postgres outbox worker lease negative smoke、0074 Postgres job worker lease primitive smoke、0075 Postgres external worker restart smoke、0076 Postgres public webhook live smoke gate、0078 Postgres worker heartbeat/polling smoke、0079 外部 secret provider evidence gate、0080 长期多副本 runtime evidence gate 已有 | 真实公网 webhook live passed evidence、外部 Vault/KMS live passed、生产密钥生命周期、长期多副本运行 live evidence 和 exactly-once | MI-NEXT-03 |
| Observability | health/ready/metrics/requestId、OTel 语义兼容本地 span、SLO/alert rules baseline | OTel SDK/collector/exporter、trace backend、alert live、error budget evidence | MI-NEXT-04 |
| Security/privacy | RBAC、secret scan、audit_event、retention policy、OWASP regression baseline | OIDC/IdP、SIEM/不可变审计存储、retention cleaner、租户隔离 live | MI-NEXT-05 |
| Bazi/Ziwei quality | L4 代表样本、evidenceRefs、snapshot/policy gate baseline | 大规模匿名 corpus、全文 diff、冲突裁决解释、人审抽样 | MI-NEXT-06 |
| External benchmark | MingLi-Bench 已登记为 evaluation_only | runner 与 license/usage 分级、nightly artifact、差异报告 | MI-NEXT-07 |
| Provider governance | provider protocol、version lock、lifecycle gate、dependency smoke 已有 | drift scanner、trace span、source/license drift、deprecation automation | MI-NEXT-08 |
| Developer platform | OpenAPI 导出、本地 sandbox、curl/Python/Node/Agent 示例已有 | 发布版 SDK/package、sandbox token、API changelog、developer portal | MI-NEXT-09 |
| Audit handoff | 任务 closeout 分散存在 | 一键聚合 audit handoff markdown/json，列明证据、未知项、外部待验证项 | MI-NEXT-10 |

### 0.6.2 100% 任务树

```text
MI-100 FateCat 测算基础设施 100%
  MI-100.01 生产外部连通闭环
    MI-100.01.01 Telegram Bot live smoke
    MI-100.01.02 生产 API/HF/current commit live gate 复跑
    MI-100.01.03 release commit registry digest/attestation 复跑
  MI-100.02 Durable runtime
    MI-100.02.01 CalculationJob event history
    MI-100.02.02 retry/timeout/non-retryable policy
    MI-100.02.03 restart recovery smoke
    MI-100.02.04 callback retry/outbox
    MI-100.02.05 external backend decision and adapter
  MI-100.03 Control plane
    MI-100.03.01 capability/provider desired-current status
    MI-100.03.02 drift scanner
    MI-100.03.03 promotion/deprecation automation
    MI-100.03.04 planned/experimental negative gates
  MI-100.04 Developer platform
    MI-100.04.01 OpenAPI release artifact and changelog
    MI-100.04.02 AsyncAPI/CloudEvents for job/webhook/evaluation events
    MI-100.04.03 SDK package or installable examples
    MI-100.04.04 sandbox token and fixed output snapshot
    MI-100.04.05 developer portal smoke
  MI-100.05 Provider platform
    MI-100.05.01 provider trace spans
    MI-100.05.02 dependency and source/license drift
    MI-100.05.03 provider compatibility matrix
    MI-100.05.04 provider deprecation and rollback
  MI-100.06 Evidence and evaluation
    MI-100.06.01 bazi corpus expansion
    MI-100.06.02 ziwei corpus expansion
    MI-100.06.03 full report golden diff
    MI-100.06.04 evidence coverage and broken-ref fail-fast
    MI-100.06.05 MingLi-Bench runner
    MI-100.06.06 conflict explainer and counter-evidence templates
  MI-100.07 SRE operations
    MI-100.07.01 OpenTelemetry SDK/collector/exporter
    MI-100.07.02 trace backend evidence
    MI-100.07.03 SLO/error budget dashboard
    MI-100.07.04 alert dry/live evidence
    MI-100.07.05 incident drill and runbook
  MI-100.08 Security/privacy
    MI-100.08.01 OIDC/IdP integration
    MI-100.08.02 tenant/authz negative tests
    MI-100.08.03 external SIEM/immutable audit storage
    MI-100.08.04 retention cleaner
    MI-100.08.05 privacy regression and LLM output scanner
  MI-100.09 Multi-surface parity
    MI-100.09.01 Web/API/Bot/CLI/Skill same profile diff
    MI-100.09.02 Markdown copy snapshot
    MI-100.09.03 Bot live report diff
    MI-100.09.04 public demo privacy guard
  MI-100.10 Audit package
    MI-100.10.01 audit handoff markdown/json
    MI-100.10.02 evidence bundle index
    MI-100.10.03 risk register and external pending list
    MI-100.10.04 third-party audit dry-run
```

### 0.6.3 执行优先级

| Priority | 任务 | 原因 | 不能跳过的证据 |
| --- | --- | --- | --- |
| P0 | MI-NEXT-03 durable runtime 二期 | 生产基础设施首先要能恢复、重试、回放和审计任务历史；0052-0060 已覆盖 SQLite/job/webhook recovery baseline，0070/0071 已覆盖 Postgres adapter 与 migration/job live smoke baseline，0072 已覆盖 Postgres outbox worker lease negative smoke，0074 已覆盖 Postgres job worker lease primitive smoke，0075 已覆盖 Postgres external worker restart smoke，0076 已覆盖 Postgres public webhook live smoke gate，0078 已覆盖 worker heartbeat/polling smoke，0079/0080 已覆盖外部 secret provider 与长期多副本证据契约门禁 | 真实公网 webhook live passed evidence、外部 Vault/KMS live passed、生产密钥生命周期、长期多副本运行 live evidence 和 exactly-once |
| P0 | MI-NEXT-04 OTel collector 与 SLO | 没有真实 trace/backend/alert，就没有生产可运维性 | trace backend URL、collector config、alert evidence |
| P0 | MI-NEXT-05 OIDC/SIEM/retention 实测 | 公开服务不能只靠本地 token 和文本 policy | IdP evidence、SIEM evidence、retention cleaner smoke |
| P0 | MI-NEXT-06 八字/紫微 corpus | 这是测算业务正确性的地基 | 匿名 corpus manifest、golden diff、evidence coverage |
| P1 | MI-NEXT-07 MingLi-Bench runner | 外部 benchmark 是质量外部信号，但不能污染生产路径 | license/usage 分级、runner summary、nightly artifact |
| P1 | MI-NEXT-08 provider drift scanner | 防止 registry 写得漂亮、实际执行漂移 | drift report、provider trace、lifecycle gate |
| P1 | MI-NEXT-09 developer portal/SDK | 基础设施必须让开发者可接入 | SDK smoke、sandbox token、API changelog |
| P0 | MI-NEXT-10 audit package | 100% 结论必须可交给第三方复核 | audit handoff、证据索引、未知项清单 |

### 0.6.4 失败判定

以下任一成立，就不能宣称 100%：

- Bot live、OIDC、SIEM、监控、告警、生产 API 等外部项没有真实证据，却写成已完成。
- capability/provider/report/job 有任意生产路径绕过统一 executor、policyGate、evidenceGate 或 provider protocol。
- release artifact 只有本地 imageId，没有当前 release commit 的 registry digest 和可验证 attestation。
- 八字/紫微只靠代表样本，没有 corpus coverage、全文 diff 或 evidence broken-ref fail-fast。
- 计划新增六爻、奇门、大六壬等功能，却没有先通过 capability 协议、provider 生命周期和评测门禁。
- 审计包无法从 Git、CI、artifact、contract、registry、script 输出逐项复核。

## 0.7 2026-07-02 Post-0060 完整实现计划

本轮继续按基础设施同构视角推进。对照 OpenAPI、AsyncAPI、CloudEvents、Kubernetes Controller、Terraform Provider、Temporal durable execution、OpenTelemetry、Google SRE SLO、OWASP API Security、NIST SSDF、NIST AI RMF、SLSA、CycloneDX 和 GitHub artifact attestations，FateCat 后续的 100% 路径应固定为“先平台能力，后功能扩张”。

| 顺序 | 实现域 | 目标状态 | 下一步可执行任务 | 不可伪造成已完成的证据 |
| --- | --- | --- | --- | --- |
| 1 | Durable runtime external backend | `CalculationJob` 从单机 SQLite baseline 升级为可恢复、可协调、可审计的外部 backend | 选型 Postgres/Temporal/Redis Queue 之一，已落 external adapter contract、migration smoke、outbox lease negative smoke、job worker lease primitive smoke、external worker restart smoke 与 public webhook live smoke gate | 长期跨副本 worker、heartbeat/polling、真实公网 webhook passed evidence、external secret provider 和 exactly-once 证据 |
| 2 | Webhook live delivery | webhook 从本地模拟器升级为真实公网 callback | 用一次性测试接收端执行公网 live smoke，校验签名、重试、outbox、隐私 | 真实接收端 URL、签名验证日志、delivery result；不得输出 secret |
| 3 | External secret storage | Fernet 本地 vault 升级为外部 Vault/KMS/secret manager | 保留本地 codec，新增外部 secret provider interface 与 dry-run/live gate | 外部 Vault/KMS 凭证、key rotation、access audit |
| 4 | AsyncAPI/CloudEvents | job/webhook/evaluation/release 事件机器可读 | 生成 AsyncAPI 或 CloudEvents schema，接入 docs smoke | event type、schema、example、version compatibility |
| 5 | OTel collector/SLO | trace/log/metrics 从本地语义 baseline 接入真实 backend | 接入 collector/exporter 配置、trace backend URL、SLO dashboard dry-run | 外部 trace 链接、error budget、alert evidence |
| 6 | OIDC/SIEM/retention | 安全从 contract baseline 升级为生产外部平台 | IdP/OIDC 接入、SIEM/不可变审计、retention cleaner smoke | 外部连通验证待执行，不能用本地 token 代替 |
| 7 | Developer platform | 开发者不读源码也能接入 | OpenAPI release artifact、SDK/package、sandbox token、API changelog | SDK smoke、sandbox token、固定输出 snapshot |
| 8 | Provider drift scanner | provider 实际执行与 registry 不漂移 | provider trace span、dependency/source/license drift report | drift scan summary、promotion/deprecation fail-fast |
| 9 | Bazi/Ziwei corpus | 核心测算质量从代表样本升级为专业回归集 | 扩八字/紫微匿名 golden corpus、全文 report diff、conflict explainer | corpus manifest、coverage matrix、人审抽样记录 |
| 10 | Audit handoff | 第三方审计可逐项复核 | 一键生成 audit handoff markdown/json、证据索引和外部待验证清单 | 当前 commit、CI、artifact、local-ci、live evidence 全部可追溯 |

当前 0060 只关闭了 “SQLite 本地 webhook outbox claim/release” 这一小块，0072 关闭 Postgres webhook outbox worker lease negative smoke，0074 关闭 Postgres job execution worker lease primitive smoke，0075 关闭 Postgres external worker restart smoke baseline，0076 关闭 Postgres public webhook live smoke gate，0078 关闭 Postgres worker heartbeat/polling smoke baseline，0079/0080 关闭 external secret provider 与 multi-replica runtime evidence gate baseline，0081 关闭 multi-replica runtime evidence assembler baseline；这些仍不能替代 production ready、exactly-once、公网 webhook live passed evidence、外部 Vault/KMS live passed 或长期多副本运行 live evidence。后续每个任务必须继续遵循：contract -> implementation -> smoke -> regression -> local-ci -> remote CI/current commit evidence -> audit note。

## 0.8 2026-07-02 Post-0060 深度调研执行蓝图

0061 对 post-0060 状态做了再次调研和任务树收敛，任务包位于 `governance/tasks/0061-measurement-infrastructure-100-post-0060-deep-research-plan/`。结论保持不变但更具体：100% 测算基础设施不是预测准确率 100%，也不是功能数量 100%，而是基础设施成熟度 100%。每个生产路径都必须做到资源可发现、接口可接入、任务可恢复、事件可订阅、质量可回归、运行可观测、安全可证明、发布可验证、审计可交接。

### 0.8.1 当前资源成熟度

| 资源 | 当前状态 | 到 100% 还缺什么 |
| --- | --- | --- |
| `Capability` | registry、profiles、production/planned 区分和 executor baseline 已有 | promotion/drift 自动化、跨端执行强一致证明 |
| `Provider` | bazi/ziwei/almanac/meihua 已接 provider protocol | trace span、dependency/source/license drift、真实外部健康探测 |
| `CalculationJob` | memory/sqlite、idempotency、cancel、event history、retry/timeout、webhook outbox、restart recovery、replayable recovery、encrypted local config vault、SQLite local lease 已有；0070 已落 Postgres ReportJobStore adapter baseline 与 SQL dry-run；0071 已落真实 Postgres migration/job smoke；0072 已落 Postgres outbox worker lease negative smoke；0074 已落 Postgres job worker lease primitive smoke；0075 已落 Postgres external worker restart smoke；0076 已落 Postgres public webhook live smoke gate；0078 已落 Postgres worker heartbeat/polling smoke；0079/0080 已落 external secret provider 与 multi-replica runtime evidence gate | 真实 webhook live passed evidence、外部 Vault/KMS live passed、长期多副本运行 live evidence 和 exactly-once |
| `Report` | JSON/Markdown、policyGate、snapshotGate、default bazi profile baseline 已有 | 全文 golden diff、人审导出、多端 semantic diff |
| `Evidence` | evidenceRefs、rule indexes、policy gate baseline 已有 | coverage report、broken-ref fail-fast、counter-evidence 自动解释 |
| `Dataset` | data-supply-chain registry、canonical/classics/vendor 分层已具备 | 大规模匿名 corpus、法律复核、长期 dataset versioning |
| `EvaluationRun` | runner/history/diff/dashboard/nightly baseline 已具备 | 长期结果库、外部 benchmark runner、趋势告警 |
| `DeliverySurface` | Web/API/Bot/CLI/Skill registry 已具备 | Bot live、SDK package、developer portal、public sandbox token |
| `AsyncEvent` | 0063 已落 CloudEvents envelope、AsyncAPI 风格 registry/document、synthetic examples 和 event contract gate baseline | 真实公网 webhook live delivery、外部 broker、事件订阅端兼容性和生产投递证据 |
| `SecurityControl` | RBAC、secret scan、audit_event、retention policy、OWASP mapping baseline 已具备；0065 已落 OIDC/SIEM/retention cleaner externalization evidence contract、negative gate 和 quick CI 接线 baseline | 真实 OIDC/IdP、外部 SIEM、不可变审计、retention cleaner live、租户隔离 |
| `ObservabilitySignal` | health/ready/metrics/requestId、OTel-compatible span log、SLO/alert rules baseline 已具备；0064 已落 OTel collector dry-run config、SLO evidence contract 和本地 gate baseline | OTel SDK/exporter、真实 collector runtime、trace backend、real error budget、alert live |
| `ReleaseArtifact` | local release artifacts、0050 GHCR digest/attestation evidence 曾完成 | 每个 release commit 复跑、签名验证、审计聚合 |
| `AuditHandoff` | 分散在任务 closeout | 一键聚合 markdown/json、证据索引、外部 pending 清单 |

### 0.8.2 后续完整实现任务树

```text
MI-100 FateCat 测算基础设施 100%
  MI-100.01 Durable Runtime
    MI-100.01.01 external backend contract and migration smoke
    MI-100.01.02 Postgres job store adapter with claim/release
    MI-100.01.03 distributed worker lease and crash recovery smoke
    MI-100.01.04 public webhook live delivery smoke
    MI-100.01.05 external Vault/KMS secret provider
  MI-100.02 Event Platform
    MI-100.02.01 CloudEvents envelope schema
    MI-100.02.02 AsyncAPI document for job/webhook/evaluation/release events
    MI-100.02.03 event compatibility and example smoke
  MI-100.03 Observability and SRE
    MI-100.03.01 OTel SDK instrumentation
    MI-100.03.02 collector/exporter config
    MI-100.03.03 trace backend evidence
    MI-100.03.04 SLO dashboard and error budget
    MI-100.03.05 alert dry/live evidence and incident drill
  MI-100.04 Security and Privacy
    MI-100.04.01 OIDC/IdP integration
    MI-100.04.02 tenant/authz negative tests
    MI-100.04.03 external SIEM or immutable audit storage
    MI-100.04.04 retention cleaner
    MI-100.04.05 privacy and LLM-output scanner
  MI-100.05 Developer Platform
    MI-100.05.01 OpenAPI release artifact and compatibility changelog
    MI-100.05.02 SDK package or installable examples
    MI-100.05.03 sandbox token service
    MI-100.05.04 developer portal smoke
  MI-100.06 Provider Platform
    MI-100.06.01 provider trace spans
    MI-100.06.02 dependency/source/license drift scanner
    MI-100.06.03 provider compatibility matrix
    MI-100.06.04 promotion/deprecation automation
  MI-100.07 Core Quality
    MI-100.07.01 bazi corpus expansion
    MI-100.07.02 ziwei corpus expansion
    MI-100.07.03 full report golden diff
    MI-100.07.04 evidence coverage and broken-ref fail-fast
    MI-100.07.05 MingLi-Bench runner and no-leak policy
    MI-100.07.06 conflict explainer and counter-evidence templates
  MI-100.08 Multi-Surface Parity
    MI-100.08.01 Web/API/Bot/CLI/Skill semantic diff
    MI-100.08.02 Markdown copy snapshot
    MI-100.08.03 Bot live report diff
    MI-100.08.04 public demo privacy guard
  MI-100.09 Release Proof
    MI-100.09.01 current commit remote CI
    MI-100.09.02 container digest/signature/attestation
    MI-100.09.03 SBOM/provenance verification
    MI-100.09.04 rollback drill
  MI-100.10 Audit Package
    MI-100.10.01 audit handoff markdown/json
    MI-100.10.02 evidence bundle index
    MI-100.10.03 risk register and external pending list
    MI-100.10.04 third-party audit dry-run
```

### 0.8.3 推荐 0062 起执行顺序

| 顺序 | 候选任务 | 最小交付物 | 不可伪造成已完成的证据 |
| --- | --- | --- | --- |
| 0062 | durable runtime external backend contract | RuntimeBackend registry/schema/gate，选 Postgres 为首个 adapter path，Temporal 为长流程 future orchestrator；0062 已落 contract baseline 与本地 gate | 不能声明 external backend 已生产 |
| 0063 | CloudEvents/AsyncAPI baseline | event schema、AsyncAPI 风格 registry/document、synthetic examples、event-contract-gate；0063 已落 contract baseline 与本地 gate | 不能只写自然语言事件说明；不能声明真实公网 webhook live delivery 或外部 broker 已生产 |
| 0064 | OTel collector/SLO adapter plan | collector dry-run config、SLO evidence contract、otel-collector-slo-gate；0064 已落 dry-run contract baseline 与本地 gate | 不能伪造 trace backend；不能声明真实 collector runtime、alert live 或 production error budget 已完成 |
| 0065 | security externalization | externalization evidence contract、security-externalization-gate、OIDC/SIEM/retention cleaner 负向伪造测试；0065 已落 contract/gate baseline | 不能用本地 token 代替 IdP；不能声明真实 OIDC、SIEM、不可变审计或 retention cleaner live 已完成 |
| 0066 | core quality corpus expansion | corpus manifest、bazi/ziwei sample expansion、full report diff policy；0066 已落 core-quality-corpus manifest、report-diff-policy、紫微匿名样本扩容和本地 gate baseline | 不能使用真实用户隐私样例；不能把工程 golden 说成专家准确率证明 |
| 0067 | developer platform | SDK/package baseline、sandbox token contract、API changelog；0067 已落 developer platform contract、本地 gate 与 metadata baseline | 不能把 docs smoke 说成发布 SDK；不能声明公网 sandbox token 服务已上线 |
| 0068 | audit handoff generator | audit bundle markdown/json generator；0068 已落 audit handoff contract、Markdown/JSON generator 和 local-ci artifact baseline | 不能遗漏 pending external validations；不能把本地 contract baseline 写成外部 live 证据 |
| 0069 | audit dry-run verifier | third-party audit dry-run verifier；0069 已落 audit dry-run contract、handoff verifier 和 local-ci artifact baseline | 不能把 dry-run passed 写成真实第三方审计完成；不能把 blocked ship gate 写成 live 通过 |
| 0070 | postgres job store adapter | Postgres ReportJobStore adapter baseline；0070 已落 tracked DDL、optional `PostgresReportJobStore`、config fail-fast、dry-run smoke 和 local-ci artifact baseline | 不能把 adapter baseline 写成真实 Postgres live、生产多副本 worker lease、exactly-once 或外部 Vault/KMS 已完成 |
| 0071 | postgres job store live smoke | Postgres migration/job live smoke baseline；0071 新增 `postgres-job-store-live-smoke.sh`、脱敏 evidence JSON、production-readiness evidence 要求和 local-ci blocked artifact baseline | 不能把一次性 DB live smoke 写成 production ready、生产多副本 worker lease、exactly-once、公网 webhook live 或外部 Vault/KMS 已完成 |
| 0072 | postgres worker lease negative smoke | Postgres webhook outbox worker lease negative smoke；0072 新增 `postgres-worker-lease-smoke.sh`、脱敏 evidence JSON、duplicate claim、错误 owner release 和 lease expiry reclaim 验证，并接入 local-ci preflight | 不能把 outbox lease smoke 写成 job execution worker lease、exactly-once、公网 webhook live 或外部 Vault/KMS 已完成 |
| 0074 | postgres job worker lease smoke | Postgres job execution worker lease primitive smoke；0074 新增 `postgres-job-worker-lease-smoke.sh`、脱敏 evidence JSON、duplicate job claim、错误 owner release、lease expiry reclaim 和 terminal job unclaimable 验证，并接入 local-ci preflight | 不能把 job lease primitive 写成 exactly-once、公网 webhook live、外部 Vault/KMS 或 production ready；external worker restart 由 0075 单独证明 |
| 0075 | postgres external worker restart smoke | Postgres external worker restart smoke；0075 新增 `postgres-external-worker-restart-smoke.sh`、脱敏 evidence JSON、stale running job、expired lease、双 manager 恢复竞争、`executionCount=1` 与 terminal lease 清理验证，并接入 local-ci preflight | 不能把 external worker restart smoke 写成 production ready、exactly-once、公网 webhook live、外部 Vault/KMS、heartbeat/polling worker 或长期多副本运行已完成 |
| 0076 | postgres public webhook live smoke gate | Postgres public webhook live smoke gate；0076 新增 `postgres-public-webhook-live-smoke.sh`、脱敏 evidence JSON、真实 Postgres DSN + 公网 HTTPS endpoint 的 live 执行入口，并接入 local-ci `--allow-missing` preflight | 不能把 gate baseline 写成 live passed evidence；无真实 DSN/URL 时只能 blocked；即使 live 通过，也不证明 production ready、exactly-once、外部 Vault/KMS、receiver SLA、heartbeat/polling worker 或长期多副本运行已完成 |
| 0078 | postgres worker heartbeat/polling smoke | Postgres worker heartbeat/polling smoke baseline；0078 新增 `postgres-worker-heartbeat-polling-smoke.sh`、`ReportJobManager` execution lease heartbeat、持久队列 polling、expired running job recovery smoke、脱敏 evidence JSON 和 local-ci `--allow-missing` preflight | 不能把 worker heartbeat/polling smoke 写成 production ready、exactly-once、公网 webhook live passed、外部 Vault/KMS 或长期多副本运行已完成 |
| 0079 | external secret provider evidence gate | 外部 secret provider/Vault/KMS evidence gate；0079 新增 `external-secret-provider-gate.sh`、外部 secret provider contract、反伪造负例和 local-ci artifact | 不能把本地 Fernet、环境变量或 placeholder proof 写成 external secret manager live passed |
| 0080 | multi-replica runtime evidence gate | 长期多副本 runtime evidence gate；0080 新增 `multi-replica-runtime-gate.sh`、`multi-replica-runtime-contract.json`、反伪造负例和 local-ci artifact | 不能把单副本、短时间 smoke、本地 SQLite 或 exactly-once overclaim 写成生产多副本 live passed |

### 0.8.4 失败判定

以下任一成立，就不能声明 FateCat 达到 100% 基础设施：

- 生产路径绕过 capability executor、provider protocol、policy gate、evidence gate 或 report profile。
- external backend、Bot live、OIDC、SIEM、OTel backend、Vault/KMS 等外部项无证据却写成已完成。
- release artifact 没有 current commit 的 CI、digest、SBOM/provenance 和 attestation verification。
- 八字/紫微只靠代表样本，没有 corpus、全文 diff、evidence coverage 或人审边界。
- 新术数 capability 直接混入默认综合八字报告，绕过 capability 协议和独立输出。
- 审计包无法从 Git、CI、artifact、contract、registry、script 输出逐项复核。

## 0.9 2026-07-03 Post-0076 深度调研完整实现计划

本轮刷新基于当前 `main` worktree、0071 Postgres migration/job live smoke 已完成事实、0072 Postgres worker lease negative smoke 已完成事实、0074 Postgres job worker lease primitive smoke 已完成事实、0075 Postgres external worker restart smoke 已完成事实、0076 Postgres public webhook live smoke gate 已完成事实，以及外部基础设施一手资料。结论是：FateCat 距离 100% 基础设施的关键缺口已经集中在“生产证据闭环”，不是继续堆术数模块。

### 0.9.1 当前事实边界

| 事实 | 当前状态 | 不能夸大的部分 |
| --- | --- | --- |
| Postgres external backend | 0070 adapter baseline、0071 migration/job live smoke baseline、0072 outbox worker lease negative smoke、0074 job worker lease primitive smoke、0075 external worker restart smoke、0076 public webhook live smoke gate、0078 worker heartbeat/polling smoke baseline、0079 external secret provider evidence gate 和 0080 multi-replica runtime evidence gate 已完成 | 仍不能声明 production ready、exactly-once、公网 webhook live passed evidence、外部 Vault/KMS live passed 或长期多副本运行 live evidence。 |
| Postgres worker lease | 0072 覆盖 webhook outbox duplicate claim、错误 owner release 和 lease expiry reclaim；0074 覆盖 queued/running job duplicate claim、错误 owner release、lease expiry reclaim 和 terminal job unclaimable；0075 覆盖 stale running job 过期 lease 后被重启 manager 恢复执行且双 manager 只有一个执行；0078 覆盖长任务 lease heartbeat、persisted queued job polling 和 expired running job polling recovery | 这仍不是 exactly-once，也不是生产多副本长期运行证据。 |
| Bot live | 0048 因缺真实 `FATE_BOT_TOKEN` 阻断 | Bot dry-run 不能替代真实 Telegram live smoke。 |
| Release proof | 0050 曾完成 GHCR digest 与 GitHub artifact attestation | 每个后续 release commit 都必须重新跑 current commit 证据。 |
| OTel/SLO/SIEM/OIDC/Vault | 当前主要是 contract、dry-run 或本地 baseline | 外部连通验证待执行。 |

### 0.9.2 外部基础设施同构依据

| 领域 | 一手资料 | FateCat 必须落实的同构能力 |
| --- | --- | --- |
| API contract | [OpenAPI Specification](https://spec.openapis.org/oas/latest.html) | 发布版 OpenAPI、错误码、changelog、SDK/package、兼容性测试。 |
| Async API | [AsyncAPI Specification](https://www.asyncapi.com/docs/reference/specification/latest) | job/webhook/evaluation/release event 需要机器可读 contract。 |
| Event envelope | [CloudEvents](https://cloudevents.io/) | 统一 event envelope，并绑定 requestId/traceId。 |
| Control plane | [Kubernetes Controllers](https://kubernetes.io/docs/concepts/architecture/controller/) | capability/provider/release gate 的 spec/status、reconciliation 和 drift scanner。 |
| Provider ecosystem | [Terraform Providers](https://developer.hashicorp.com/terraform/language/providers) | providerId、engineVersion、source/license、health、deprecation 和 rollback。 |
| Durable workflow | [Temporal Durable Execution](https://docs.temporal.io/evaluate/understanding-temporal) | CalculationJob event history、external backend、retry/timeout、worker lease、crash recovery。 |
| Database coordination | [PostgreSQL SELECT / SKIP LOCKED](https://www.postgresql.org/docs/current/sql-select.html) | Postgres outbox/job 多 worker claim negative smoke 和 lease expiry reclaim。 |
| Observability | [OpenTelemetry Signals](https://opentelemetry.io/docs/concepts/signals/) | API -> job -> provider -> report trace、metrics、logs 与 backend evidence。 |
| SRE | [Google SRE SLO](https://sre.google/sre-book/service-level-objectives/) | availability、latency、job success、provider failure、error budget 和 alert。 |
| Platform engineering | [CNCF Platform Engineering Maturity Model](https://tag-app-delivery.cncf.io/whitepapers/platform-eng-maturity-model/) | FateCat 必须被开发者和 Agent 自助发现、接入、验证和度量。 |
| API security | [OWASP API Security Top 10 2023](https://owasp.org/API-Security/editions/2023/en/0x11-t10/) | OIDC/RBAC、tenant isolation、rate/body limit、API inventory、negative tests。 |
| Secure development | [NIST SSDF SP 800-218](https://csrc.nist.gov/pubs/sp/800/218/final) | secret scan、dependency review、release evidence、incident response。 |
| AI risk | [NIST AI RMF](https://www.nist.gov/itl/ai-risk-management-framework) | AI/Agent 只解释结构化结果，不重算命盘，不输出高风险替代建议。 |
| Supply chain | [SLSA v1.2](https://slsa.dev/spec/v1.2/)、[CycloneDX](https://cyclonedx.org/specification/overview/) | current commit CI、container digest、SBOM/provenance、attestation verify。 |
| Artifact proof | [GitHub Artifact Attestations](https://docs.github.com/en/actions/how-tos/secure-your-work/use-artifact-attestations/use-artifact-attestations) | 每个 release commit 重新生成并验证 attestation。 |
| Idempotency/Webhooks | [Stripe Idempotent requests](https://docs.stripe.com/api/idempotent_requests)、[Stripe Webhooks](https://docs.stripe.com/webhooks) | report job idempotency、webhook signature、retry/outbox、event audit。 |

### 0.9.3 资源成熟度矩阵

| Resource Domain | 当前状态 | 100% 目标 | 下一证据 |
| --- | --- | --- | --- |
| Capability Control Plane | registry/executor/profiles 已有 | spec/status、admission、promotion、deprecation、drift scanner 自动化 | capability/provider drift scanner |
| Provider Platform | production provider baseline 已有 | provider trace、dependency health、source/license drift、compatibility matrix、rollback | provider trace span + drift report |
| CalculationJob Durable Runtime | memory/sqlite + Postgres adapter/live smoke + Postgres outbox worker lease negative smoke + Postgres job worker lease primitive smoke + Postgres external worker restart smoke + Postgres public webhook live smoke gate + Postgres worker heartbeat/polling smoke baseline + external secret provider evidence gate + multi-replica runtime evidence gate | 公网 webhook live passed evidence、external secret provider live passed、长期多副本运行 live evidence、at-least-once + idempotency 和 exactly-once 边界 | 下一步做真实 webhook live evidence、external secret provider live evidence 或长期多副本 runtime live drill |
| Event Platform | CloudEvents/AsyncAPI baseline 已有 | producer/consumer contract tests、event versioning、dead-letter、replay/audit | event compatibility smoke |
| Report/Evidence | policyGate、snapshotGate、evidenceRefs baseline | 全文 golden diff、evidence coverage、broken-ref fail-fast、counter-evidence | report diff runner + evidence coverage gate |
| Core Quality | 八字/紫微 L4 代表样本 | 大规模匿名 corpus、MingLi-Bench runner、人审抽样、趋势报告 | corpus expansion + external benchmark runner |
| Developer Platform | OpenAPI/example/sandbox baseline | 发布 SDK/package、developer portal、sandbox token、fixed snapshot、compat changelog | SDK smoke + portal docs smoke |
| Observability/SRE | local OTel-compatible span、SLO/alert rules | OTel SDK/collector/exporter/backend、dashboards、error budget、alert live、incident drill | collector/backend evidence |
| Security/Privacy | token/RBAC/secret scan/audit_event/retention baseline | OIDC/IdP、tenant isolation、SIEM/immutable audit、retention cleaner、privacy/LLM scanner | OIDC/SIEM live or staged evidence |
| Supply Chain/Release | local artifacts + prior GHCR/attestation baseline | every release commit has digest、SBOM/provenance、attestation verify、rollback drill | current commit release proof run |
| Multi-Surface Delivery | Web/API/Bot/CLI/Skill registry | semantic diff across surfaces、Bot live、HF/API live、Markdown copy snapshot | multi-surface semantic diff smoke |
| Audit Handoff | generator/dry-run baseline | one-command audit bundle with evidence index, pending externals, risk register, third-party review | audit bundle for current commit |

### 0.9.4 完整实现任务树

```text
MI-100 FateCat 测算基础设施 100%
  MI-100.01 Durable Runtime
    MI-100.01.01 Postgres job execution worker lease primitive
    MI-100.01.02 crash/restart recovery with external backend worker
    MI-100.01.03 public webhook live delivery smoke
    MI-100.01.04 external Vault/KMS or secret manager provider
    MI-100.01.05 delivery semantics: at-least-once + idempotency + duplicate negative tests
  MI-100.02 Control Plane
    MI-100.02.01 capability/provider spec-status reconciliation
    MI-100.02.02 drift scanner for registry vs runtime behavior
    MI-100.02.03 promotion/deprecation automation
    MI-100.02.04 planned/experimental negative execution gates
  MI-100.03 Event Platform
    MI-100.03.01 event version compatibility matrix
    MI-100.03.02 webhook consumer contract fixture
    MI-100.03.03 dead-letter and replay policy
    MI-100.03.04 event audit and trace linkage
  MI-100.04 Provider Platform
    MI-100.04.01 provider trace spans
    MI-100.04.02 dependency/source/license drift scanner
    MI-100.04.03 provider compatibility matrix
    MI-100.04.04 provider rollback and deprecation runbook
  MI-100.05 Core Quality and Evaluation
    MI-100.05.01 bazi corpus expansion
    MI-100.05.02 ziwei corpus expansion
    MI-100.05.03 full report golden diff
    MI-100.05.04 evidence coverage and broken-ref fail-fast
    MI-100.05.05 MingLi-Bench runner and no-leak policy
    MI-100.05.06 conflict explainer and counter-evidence templates
  MI-100.06 Developer Platform
    MI-100.06.01 release OpenAPI artifact and compatibility changelog
    MI-100.06.02 SDK package or installable examples
    MI-100.06.03 sandbox token service
    MI-100.06.04 developer portal smoke
  MI-100.07 Observability and SRE
    MI-100.07.01 OTel SDK instrumentation
    MI-100.07.02 collector/exporter config
    MI-100.07.03 trace backend evidence
    MI-100.07.04 SLO dashboard and error budget
    MI-100.07.05 alert dry/live evidence and incident drill
  MI-100.08 Security and Privacy
    MI-100.08.01 OIDC/IdP integration
    MI-100.08.02 tenant/authz negative tests
    MI-100.08.03 external SIEM or immutable audit storage
    MI-100.08.04 retention cleaner
    MI-100.08.05 privacy and LLM-output scanner
  MI-100.09 Multi-Surface Parity
    MI-100.09.01 Web/API/Bot/CLI/Skill semantic diff
    MI-100.09.02 Markdown copy snapshot
    MI-100.09.03 Bot live report diff
    MI-100.09.04 public demo privacy guard
  MI-100.10 Supply Chain and Release Proof
    MI-100.10.01 current commit remote CI
    MI-100.10.02 container digest/signature/attestation
    MI-100.10.03 SBOM/provenance verification
    MI-100.10.04 rollback drill
  MI-100.11 Audit Package
    MI-100.11.01 audit handoff markdown/json
    MI-100.11.02 evidence bundle index
    MI-100.11.03 risk register and external pending list
    MI-100.11.04 third-party audit dry-run
```

### 0.9.5 推荐执行顺序

| 顺序 | 任务 | 原因 | 最小证据 |
| --- | --- | --- | --- |
| 1 | Public webhook live smoke | callback 是外部副作用，必须真实公网验证签名/重试/隐私 | disposable receiver URL、signed event verified、secret redacted |
| 2 | External secret provider | 本地 Fernet 不等于生产密钥生命周期 | Vault/KMS/secret manager dry-run + live evidence |
| 3 | Worker heartbeat/polling hardening | 0075 只证明过期 lease 后的一次恢复 smoke，还不是长期 worker runtime | heartbeat/renew、DB polling、lease expiry backoff、stuck job recovery smoke |
| 4 | OTel backend and SLO | 无 trace backend 和 error budget 就没有生产可运维性 | collector/exporter config、trace backend link、SLO evidence |
| 5 | OIDC/SIEM/retention | 公开服务需要真实身份和审计闭环 | IdP evidence、SIEM event evidence、retention cleaner smoke |
| 6 | Core corpus and benchmark | 基础设施不能只稳定运行，还要稳定正确 | bazi/ziwei corpus、MingLi-Bench runner、report diff |
| 7 | Developer platform release | 基础设施必须让第三方开发者可接入 | SDK/package smoke、developer portal docs smoke、sandbox token |
| 8 | Current release proof | 每个 release commit 都需要不可伪造证据 | current commit CI、digest、SBOM/provenance、attestation verify |
| 9 | Audit package | 100% 结论要能给第三方复核 | audit bundle markdown/json、risk register、pending external list |

### 0.9.6 100% 完成门禁

- 所有 production capability 必须通过同一个 executor、provider protocol、policy gate、evidence gate 和 report profile。
- 所有异步 job 必须有 durable backend、event history、idempotency、retry/timeout、outbox、签名、审计和恢复策略。
- 所有外部依赖必须有真实证据；没有证据只能写“外部连通验证待执行”。
- 所有核心测算质量必须有 golden、snapshot、benchmark、coverage、人审边界和差异报告。
- 所有发布结论必须能从 Git、CI、artifact、contract、registry、script output 逐项复核。
- 所有安全边界必须覆盖认证、授权、租户隔离、限流、secret scan、SIEM、retention 和隐私回归。

### 0.9.7 失败判定

以下任一成立，就不能声明 FateCat 达到 100% 基础设施：

- 0072 或任何 outbox lease smoke 被写成 job execution worker lease、exactly-once 或生产完成。
- dry-run、本地 smoke、contract gate 被写成真实 live。
- external backend、Bot live、OIDC、SIEM、OTel backend、Vault/KMS、公网 webhook 无证据却写成完成。
- 新术数能力绕过 capability/provider/evidence/report 协议进入默认报告。
- 八字/紫微只靠少量代表样本，没有 corpus、全文 diff、evidence coverage 或人审边界。
- release artifact 没有 current commit 的 CI、digest、SBOM/provenance 和 attestation verification。
- 审计包无法从 Git、CI、artifact、contract、registry、script output 逐项复核。

## 0.10 2026-07-03 Post-0076 实现计划刷新

0077 对 post-0076 状态做了 planning-only 调研，任务包位于 `governance/tasks/0077-measurement-infrastructure-100-post-0076-deep-research-plan/`。结论：FateCat 后续不是继续堆新术数模块，而是先关闭基础设施生产证据链。0076 已建立 Postgres public webhook live smoke gate，0078 已建立 worker heartbeat/polling baseline，0079/0080 已建立 external secret provider 与 multi-replica runtime evidence gate baseline，但真实公网 webhook passed evidence、外部 Vault/KMS live passed、长期多副本运行 live evidence、exactly-once 边界、Bot live、OIDC、SIEM、OTel backend 和 current release proof 仍未完成。

### 0.10.1 100% 基础设施终态

FateCat 达到 100% 时必须同时满足：

- `Capability` 可发现、可准入、可拒绝、可执行、可审计。
- `Provider` 有来源、版本、license、health、trace、drift scanner 和 deprecation/rollback。
- `CalculationJob` 有 external backend、event history、retry/timeout、worker lease、heartbeat、polling、outbox、签名、审计和恢复策略。
- `Event` 有 CloudEvents/AsyncAPI 契约、版本、兼容性测试、死信/回放策略和真实投递证据。
- `Report/Evidence` 有全文 golden diff、evidence coverage、broken-ref fail-fast、冲突和反证说明。
- `EvaluationRun` 有八字/紫微 corpus、MingLi-Bench runner、趋势记录和 no-leak policy。
- `Observability` 有 OpenTelemetry exporter/backend、SLO、error budget、alert 和 incident drill。
- `Security` 有 OIDC/RBAC、tenant isolation、SIEM/immutable audit、retention cleaner 和 privacy/LLM-output scanner。
- `ReleaseArtifact` 有 current commit CI、container digest、SBOM/provenance、attestation verify 和 rollback evidence。
- `AuditHandoff` 能一键聚合当前 commit 的 evidence index、risk register 和 pending external validations。

### 0.10.2 外部依据与同构映射

| 成熟基础设施范式 | 一手资料 | FateCat 对应能力 |
| --- | --- | --- |
| API contract | [OpenAPI Specification](https://spec.openapis.org/oas/latest.html) | OpenAPI release artifact、错误码、changelog、SDK smoke。 |
| Async API | [AsyncAPI Specification](https://www.asyncapi.com/docs/reference/specification/latest) | job/webhook/evaluation/release events 的机器可读契约。 |
| Event envelope | [CloudEvents](https://cloudevents.io/) | 统一 `id/source/type/subject/time/data`，绑定 requestId/traceId。 |
| Control plane | [Kubernetes Controllers](https://kubernetes.io/docs/concepts/architecture/controller/) | capability/provider/release gate 的 spec/status、reconciliation、drift scanner。 |
| Provider ecosystem | [Terraform Providers](https://developer.hashicorp.com/terraform/language/providers) | providerId、engineVersion、source/license、health、deprecation、rollback。 |
| Durable workflow | [Temporal Durable Execution](https://docs.temporal.io/evaluate/understanding-temporal) | external backend、event history、retry/timeout、worker lease、crash recovery。 |
| Observability | [OpenTelemetry Signals](https://opentelemetry.io/docs/concepts/signals/) | API -> job -> provider -> report trace、metrics、logs 和 backend evidence。 |
| SRE | [Google SRE SLO](https://sre.google/sre-book/service-level-objectives/) | availability、latency、job success、provider failure、error budget、alert/runbook。 |
| Delivery metrics | [DORA Metrics](https://dora.dev/guides/dora-metrics-four-keys/) | release gate、rollback drill、change failure 和 MTTR 记录。 |
| Platform engineering | [CNCF Platform Engineering Maturity Model](https://tag-app-delivery.cncf.io/whitepapers/platform-eng-maturity-model/) | 开发者和 Agent 自助发现、接入、验证、度量。 |
| Software catalog | [Backstage System Model](https://backstage.io/docs/features/software-catalog/system-model/) | Capability、Provider、Dataset、EvaluationRun、SecurityControl、DeliverySurface 目录化。 |
| API security | [OWASP API Security Top 10 2023](https://owasp.org/API-Security/editions/2023/en/0x11-t10/) | OIDC/RBAC、tenant isolation、rate/body limit、API inventory、negative tests。 |
| Secure development | [NIST SSDF SP 800-218](https://csrc.nist.gov/pubs/sp/800/218/final) | secret scan、dependency review、release evidence、incident response。 |
| Supply chain | [SLSA v1.2](https://slsa.dev/spec/v1.2/)、[CycloneDX](https://cyclonedx.org/specification/overview/) | current commit CI、container digest、SBOM/provenance、attestation verify。 |
| Idempotency/webhooks | [Stripe idempotent requests](https://docs.stripe.com/api/idempotent_requests)、[Stripe webhooks](https://docs.stripe.com/webhooks) | report job idempotency、webhook signature、retry/outbox、event audit。 |

### 0.10.3 后续实现队列

| 顺序 | 建议任务 | 执行条件 | 最小证据 |
| --- | --- | --- | --- |
| 0078 | Postgres worker heartbeat/polling hardening | 本地可执行，不依赖外部平台 | heartbeat renew、DB polling、lease expiry backoff、stuck job recovery smoke。 |
| 0079 | External secret provider interface and gate | 已完成本地 evidence contract/gate；live 需外部 Vault/KMS | `external-secret-provider-gate.sh`、反伪造负例、live evidence contract。 |
| 0080 | Multi-replica runtime evidence gate | 已完成本地 evidence contract/gate；live 需真实多副本运行环境 | `multi-replica-runtime-gate.sh`、反伪造负例、24h/2 副本/100 jobs live evidence contract。 |
| 0081 | Multi-replica runtime evidence assembler | 已完成本地 pending/live fixture assembler；live 仍需真实多副本运行环境 | `multi-replica-runtime-evidence-assembler.sh`、pending artifact、gate-compatible redacted live evidence JSON。 |
| 0082 | OTel backend/SLO staged gate | 已完成本地 staged evidence contract/gate；backend live 需外部平台 | `otel-backend-slo-gate.sh`、反伪造负例、pending artifact；真实 collector runtime、trace backend、metrics backend、SLO dashboard、alert live、error budget 和 incident drill 仍待外部证据。 |
| 0083 | OIDC/SIEM/retention staged gate | 已完成本地 staged gate 加固；live 需 IdP/SIEM/retention cleaner 权限 | `security-externalization-gate.sh`、proof-ref 白名单、raw URL 禁入、production deletion marker 拒绝、5 类反伪造负例；真实 OIDC/SIEM/retention cleaner live evidence 仍待外部证据。 |
| 0084 | Provider drift scanner | 已完成本地 drift scanner baseline；真实公网外部依赖 live smoke 和法律复核仍需人工/外部权限 | `provider-drift-scanner.sh`、provider.validate/provider.calculate span、dependency/source/license/vendor drift report、quick CI artifact。 |
| 0085 | Core corpus and MingLi-Bench runner | 已完成本地 core corpus + MingLi-Bench aggregate gate baseline；外部模型评测和专家人工准确率仍未完成 | `mingli-bench-gate.sh`、core corpus summary、FateCat baseline aggregate、license/usage/no-leak policy、quick CI artifact。 |
| 0086 | Developer portal and SDK release baseline | 已完成本地 release-readiness baseline；公网 portal、PyPI/npm 和 sandbox token 仍需外部部署 | `developer-portal-gate.sh`、SDK package smoke、developer docs smoke、sandbox fixed snapshot；不声明公网 live。 |
| 0090 | Multi-surface semantic diff | 已落本地 normalized semantic hash gate；Bot live/HF Space live 仍需外部凭证和真实 URL | `multi-surface-semantic-diff.sh`、API direct/API job/Web direct/Web job/Bot dry-run hash 对齐、Bot static chain check；0094 已补 CLI/Skill non-Markdown evidence surfaces、external pending list。 |
| 0091 | Retention cleanup baseline | 已落本地 SQLite records/report jobs dry-run/execute smoke；生产 scheduler、Postgres production cleanup live 和外部 SIEM retention 仍待外部证据 | `retention-cleanup-smoke.sh`、`contracts/fate/security/retention-cleanup.json`、production-security-gate/local-ci 接线、summary 脱敏断言。 |
| 0088 | Current release proof | 已落 current-release-proof gate；每个最终 release commit 仍需 commit/push 后执行远端 proof | `current-release-proof.sh`、remote CI、container digest、SBOM/provenance、attestation verify、rollback evidence。 |
| 0089 | Current audit bundle | 已落 current-audit-bundle gate；final release commit 仍需 commit/push 后执行 required proof | `current-audit-bundle.sh`、audit handoff markdown/json、audit dry-run、current release proof、release artifacts、rollback evidence、evidence index、risk register、pending external list。 |

如果用户提供真实 `FATE_REPORT_JOB_DATABASE_URL` + 公网 HTTPS webhook receiver，或真实 `FATE_BOT_TOKEN`，对应 live evidence 任务可以插队；否则统一标记 `外部连通验证待执行`。

### 0.10.4 暂停范围

- 暂不新增六爻、奇门、大六壬、塔罗等 production capability。
- 暂不把 planned capability 接入默认综合八字报告。
- 暂不声明 exactly-once；当前目标应写成 at-least-once + idempotency + duplicate negative tests。
- 暂不把 local smoke、dry-run、contract gate、allow-missing blocked summary 写成生产 live evidence。

### 0.10.5 100% 完成门禁

- 所有 production capability 均通过同一个 executor、provider protocol、policy gate、evidence gate 和 report profile。
- 所有异步 job 均具备 durable backend、event history、idempotency、retry/timeout、outbox、签名、审计、恢复和 worker heartbeat/polling。
- 所有外部依赖均有真实证据；没有证据只能写 `外部连通验证待执行`。
- 所有核心测算质量均有 golden、snapshot、benchmark、coverage、人审边界和差异报告。
- 所有发布结论均能从 Git、CI、artifact、contract、registry、script output 逐项复核。
- 所有安全边界均覆盖认证、授权、租户隔离、限流、secret scan、SIEM、retention 和隐私回归。
- 审计包能交给第三方从当前 commit 复核，不依赖聊天记录。

### 0.10.6 失败判定

以下任一成立，不能声明 FateCat 达到 100% 基础设施：

- dry-run、本地 smoke、allow-missing blocked summary 或 contract gate 被写成真实 live passed。
- Postgres public webhook live smoke gate 被写成公网 webhook live passed evidence。
- external backend、Bot live、OIDC、SIEM、OTel backend、Vault/KMS、长期多副本运行无证据却写成完成。
- 新术数能力绕过 capability/provider/evidence/evaluation/security/release/audit 协议进入默认报告。
- 八字/紫微只靠少量代表样本，没有 corpus、全文 diff、evidence coverage 或人审边界。
- release artifact 没有 current commit 的 CI、digest、SBOM/provenance 和 attestation verification。
- 审计包无法从 Git、CI、artifact、contract、registry、script output 逐项复核。

## 0.11 2026-07-03 Post-0091 深度调研完整实现计划

0092 对 post-0091 状态做 planning-only 调研，任务包位于 `governance/tasks/0092-measurement-infrastructure-100-post-0091-deep-research-plan/`。结论：0091 已把本地 SQLite records/report jobs retention cleanup baseline 接进 production-security/local-ci，但这不等于生产 scheduler、Postgres production cleanup live 或外部 SIEM/log retention 完成。当前 `44cbedd` 的 GitHub Acceptance run `28657479378` 和 Container run `28657481029` 均为 success；0092 文档变更提交后仍需重新跑当前提交证据。

### 0.11.1 Post-0091 当前事实

| 域 | 已有完成面 | 仍不能声明完成的项 |
| --- | --- | --- |
| Release proof | 0088 已有 current release proof gate；0091 提交 `44cbedd` 的 Acceptance/Container 远端 CI 已通过 | 后续每个 release commit 仍需重新收集远端 CI、digest、attestation、SBOM/provenance 和 rollback evidence |
| Audit bundle | 0089 已有 current audit bundle generator 和 required proof gate | 不替代第三方审计；外部 live evidence 未关闭时必须继续列 pending |
| Multi-surface parity | 0090 已验证 API direct/API job/Web direct/Web job/Bot dry-run normalized semantic hash 同源 | CLI/Skill 仍未纳入同源 diff；Bot live 仍需真实 token |
| Retention | 0091 已完成本地 SQLite records/report jobs dry-run/execute cleanup smoke | 生产 scheduler、Postgres production cleanup live、外部 SIEM/log retention 仍待外部证据 |
| Core quality | 0085 已有 MingLi-Bench aggregate gate 和 core corpus baseline | 大规模匿名 corpus、全文 report diff、人审抽样和冲突反证表达仍需扩容 |
| Developer platform | 0086/0087 已有 developer portal/SDK readiness 和 sandbox gateway baseline | 公网 portal、发布版 SDK、公网 token issuer/revocation 仍待外部发布 |
| Observability/SRE | 0082 已有 OTel backend/SLO staged evidence gate | 真实 OTel backend、alert live、error budget、incident drill 仍待外部平台证据 |
| Security/privacy | 0083 已有 OIDC/SIEM/retention staged gate；0091 已有本地 cleanup baseline | 真实 OIDC/IdP、SIEM/immutable audit、生产 retention cleaner 仍待外部连通验证 |

### 0.11.2 同构资料刷新

| 成熟基础设施范式 | 一手资料 | Post-0091 对 FateCat 的要求 |
| --- | --- | --- |
| OpenAPI | https://spec.openapis.org/oas/latest.html | OpenAPI 3.2.0 latest 入口作为 API 契约目标；SDK/portal 需要可由契约生成或校验。 |
| AsyncAPI | https://www.asyncapi.com/docs/reference/specification/latest | AsyncAPI 3.1.0 latest 入口作为事件契约目标；job/webhook/evaluation/release 事件需要 producer/consumer contract tests。 |
| CloudEvents | https://cloudevents.io/ | 事件 envelope 必须统一 `id/source/type/subject/time/data`，并绑定 trace/request metadata。 |
| Kubernetes Controller | https://kubernetes.io/docs/concepts/architecture/controller/ | capability/provider/security/release 等资源需要 desired/current、status 和 drift scanner。 |
| Terraform Provider | https://developer.hashicorp.com/terraform/language/providers | 每个测算 provider 必须有 version、source/license、health、deprecation、compat matrix。 |
| Temporal Durable Execution | https://docs.temporal.io/evaluate/understanding-temporal | CalculationJob 需要 external backend、event history、heartbeat、retry/timeout 和 crash recovery。 |
| OpenTelemetry Signals | https://opentelemetry.io/docs/concepts/signals/ | API -> job -> provider -> report 必须能落 traces/metrics/logs，并最终有外部 backend evidence。 |
| Google SRE SLO | https://sre.google/sre-book/service-level-objectives/ | 100% 生产口径必须有 SLI/SLO/error budget、alert 和 incident drill，不是只看 smoke pass。 |
| OWASP API Security 2023 | https://owasp.org/API-Security/editions/2023/en/0x11-t10/ | 公开 API 必须继续覆盖授权、认证、资源消耗、库存、配置和负向回归。 |
| NIST SSDF | https://csrc.nist.gov/pubs/sp/800/218/final | 安全开发证据必须覆盖 secret scan、dependency review、release evidence 和 incident response。 |
| SLSA / CycloneDX / GitHub Attestation | https://slsa.dev/spec/v1.2/、https://cyclonedx.org/specification/overview/、https://docs.github.com/en/actions/how-tos/secure-your-work/use-artifact-attestations/use-artifact-attestations | 每个 release commit 必须有 current CI、container digest、SBOM/provenance 和 attestation verify 证据。 |
| CNCF Platform Engineering / Backstage | https://tag-app-delivery.cncf.io/whitepapers/platform-eng-maturity-model/、https://backstage.io/docs/features/software-catalog/system-model/ | FateCat 必须让开发者和 Agent 自助发现、接入、验证、观测 capability，不读源码也能用。 |
| Stripe Idempotency/Webhooks | https://docs.stripe.com/api/idempotent_requests、https://docs.stripe.com/webhooks | 外部副作用必须用幂等、签名、重试、outbox 和事件历史治理。 |

### 0.11.3 Post-0091 下一批推荐任务

| 顺序 | 建议任务 | 为什么现在做 | 阻断/前置 |
| --- | --- | --- | --- |
| 0093 | CLI capability command baseline | 已完成：新增根级 `scripts/capability-cli.sh`、机器可读 smoke、delivery contract、registry/local-ci/docs/tests 接线；保持 `surface.cli.status=partial`，不声明 Markdown 多端同源或外部 live | 无外部依赖 |
| 0094 | CLI/Skill semantic diff expansion | 已完成：`multi-surface-semantic-diff` 继续验证 API/Web/Bot 标准 Markdown hash 同源，并新增 CLI capability smoke 与 Skill command chain non-Markdown evidence；不声明 CLI/Skill 生成标准 Markdown 或外部 live 已完成 | 0093 已完成 |
| 0095 | post-0094 deep research plan refresh | 已完成：把 0093/0094 后的资源成熟度矩阵、执行波次和不可伪造证据口径落盘 | 无外部依赖 |
| 0096 | 八字/紫微 corpus/report diff expansion | 当前执行：紫微 basic corpus 从 4 扩到 8 个匿名北京样本，core-quality gate 增加覆盖标签和 summary-only report diff 策略 | 匿名样本和版权/隐私边界 |
| Next | Retention production cleanup staged gate | 0091 只完成本地 SQLite cleanup；生产还需要 scheduler、Postgres cleanup 和 SIEM/log retention 证据 | 真实 Postgres/调度器/SIEM 可选；无外部环境时输出 pending |
| Next | Event platform consumer/replay contract tests | 已有事件契约但缺消费者兼容、DLQ/replay 策略和负向测试 | 无外部 broker 时先做本地 contract tests |
| Next | External live evidence pack | 如果用户提供真实凭证，应集中收集 live 证据而不是散落在聊天里 | Bot token、公网 webhook、IdP/SIEM/OTel/Vault、多副本窗口 |
| Next | Current release proof rerun | 每个 release commit 都要重新证明，不可沿用旧 commit 证据 | commit/push 后执行 |
| Next | Current audit bundle rerun | 100% 结论必须可交给第三方复核 | current release proof 和当前 audit handoff |

### 0.11.4 本地可执行与外部待执行分层

| Type | Items | Rule |
| --- | --- | --- |
| 本地可立即执行 | event contract tests、core corpus 扩容、report diff、current release proof/audit bundle rerun | 0093 CLI capability command、0094 CLI/Skill diff 与 0095 post-0094 plan 已完成；0096 正在推进 core corpus/report diff。 |
| 需要外部环境但可先做 gate | Postgres cleanup live gate、retention scheduler proof contract、OTel/SLO evidence gate、OIDC/SIEM evidence gate、Vault/KMS evidence gate | 无凭证时输出 blocked/pending summary，不能写 live passed。 |
| 只能由真实外部证据关闭 | Telegram Bot live、公网 webhook passed、外部 SIEM ingestion、OIDC/IdP live、OTel backend query、长期多副本运行、生产 rollback | 统一标记 `外部连通验证待执行`。 |

### 0.11.5 100% 完成门禁

- 所有 production capability 均通过同一个 executor、provider protocol、policy gate、evidence gate 和 report profile。
- 所有 delivery surface 均有同源输出证明；CLI/Skill 不能长期停留在 registry 文本说明。
- 所有异步 job 均具备 durable backend、event history、idempotency、retry/timeout、outbox、签名、审计、恢复、worker heartbeat/polling 和生产 cleanup 策略。
- 所有核心测算质量均有 golden、snapshot、benchmark、coverage、人审边界、全文 diff 和冲突反证说明。
- 所有安全边界均覆盖认证、授权、租户隔离、限流、secret scan、SIEM、retention、隐私回归和 LLM 输出扫描。
- 所有外部结论均有真实 evidence；没有证据只能写 `外部连通验证待执行`。
- 审计包能从当前 Git、CI、artifact、contract、registry、script output 逐项复核，不依赖聊天记录。

### 0.11.6 失败判定

以下任一成立，不能声明 FateCat 达到 100% 基础设施：

- 0091 retention local cleanup 被写成生产 scheduler、Postgres production cleanup live 或 SIEM/log retention 已完成。
- 0090 API/Web/Bot dry-run diff 被写成 CLI/Skill 同源已完成。
- dry-run、本地 smoke、allow-missing blocked summary 或 contract gate 被写成真实 live passed。
- external backend、Bot live、OIDC、SIEM、OTel backend、Vault/KMS、长期多副本运行无证据却写成完成。
- 新术数能力绕过 capability/provider/evidence/evaluation/security/release/audit 协议进入默认报告。
- 八字/紫微只靠少量代表样本，没有 corpus、全文 diff、evidence coverage 或人审边界。
- release artifact 没有 current commit 的 CI、digest、SBOM/provenance 和 attestation verification。
- 审计包无法从 Git、CI、artifact、contract、registry、script output 逐项复核。

## 1. 调研结论

成熟基础设施产品的共性不是“功能多”，而是把复杂领域压成可发现、可执行、可审计、可观测、可回滚的稳定资源。

| 参考领域 | 官方依据 | FateCat 同构要求 |
| --- | --- | --- |
| API 基础设施 | [OpenAPI Specification](https://spec.openapis.org/oas/latest.html) 定义 HTTP API 的标准、语言无关接口描述 | 对外 API 必须有机器可读契约、错误码、schema、示例和版本策略 |
| 支付 API | [Stripe Idempotent requests](https://docs.stripe.com/api/idempotent_requests) 使用幂等键避免重复副作用 | 报告 job、记录写入、callback 必须支持幂等、防重复和参数一致性校验 |
| 持久工作流 | [Temporal Durable Execution](https://docs.temporal.io/evaluate/understanding-temporal) 把长流程状态交给平台保存和恢复 | 长报告、多 provider 组合、批量评测要从内存任务升级为可恢复状态机 |
| 重试策略 | [Temporal Retry Policies](https://docs.temporal.io/encyclopedia/retry-policies) 将重试声明化 | provider 外部依赖、webhook、评测 runner 需要声明式 retry/timeout/non-retryable policy |
| 软件目录 | [Backstage System Model](https://backstage.io/docs/features/software-catalog/system-model/) 用 Component/API/Resource 建模系统 | Capability、Provider、Dataset、EvaluationRun、SecurityControl、DeliverySurface 必须可发现 |
| 控制面 | [Kubernetes Controllers](https://kubernetes.io/docs/concepts/architecture/controller/) 通过 desired state/current state reconciliation 管理资源 | FateCat 需要 admission gate、status、ready、policy gate，而不是散落 if/else |
| Provider 生态 | [Terraform Providers](https://developer.hashicorp.com/terraform/language/providers) 把外部能力插件化、版本化、锁定化 | 八字、紫微、黄历、梅花等 provider 必须声明来源、版本、健康、许可证和能力边界 |
| 可观测性 | [OpenTelemetry Signals](https://opentelemetry.io/docs/concepts/signals/) 统一 traces、metrics、logs | API -> job -> provider -> report builder 必须能串起 trace、metrics、structured logs |
| SRE | [Google SRE SLO](https://sre.google/sre-book/service-level-objectives/) 用 SLO/error budget 管理可靠性 | 需要 p95/p99、错误率、队列积压、provider failure rate、release budget |
| 供应链安全 | [SLSA v1.2](https://slsa.dev/spec/v1.2/) 用 levels、provenance、attestation 改进供应链安全 | 需要 release provenance、SBOM、vendor/source/license/data manifest、artifact verification |
| API 安全 | [OWASP API Security Top 10 2023](https://owasp.org/API-Security/editions/2023/en/0x11-t10/) 明确 BOLA、认证、资源消耗、配置、库存等风险 | 记录接口、管理接口、callback、provider 外部调用必须有 RBAC、限流、审计和 API inventory |
| ML 平台 | [MLflow Model Registry](https://mlflow.org/docs/latest/ml/model-registry/) 强调版本、lineage、治理和生产切换 | 测算规则、provider、报告 profile、golden dataset 应有版本、lineage、promotion gate |

结论：FateCat 的 100% 不是把六爻、奇门、大六壬全部堆进去，而是让每个测算体系进入同一套能力协议、provider 生命周期、证据模型、评测门禁和生产运行控制面。

## 2. 目标终态

FateCat 100% 测算基础设施应具备五层架构：

```text
Client / Agent / Web / Bot / CLI / Skill
  -> Delivery Surface / API Contract / Auth
  -> Capability Control Plane
  -> Calculation Job / Durable Runtime
  -> Provider Adapter / Mature Engine / Data Source
  -> Evidence Builder / Report Builder
  -> Evaluation / Observability / Security / Supply Chain Gates
```

终态原则：

- 所有体系都是 `Capability`，不能绕过 registry 执行。
- 所有生产能力都有 `Provider`，不能靠散落函数和前端拼接。
- 所有长流程都是 `CalculationJob`，不能依赖单进程内存完成生产可靠性。
- 所有报告都是 `Report`，JSON/Markdown/profile/evidence/policy gate 同源。
- 所有结论必须能追溯 evidence，不允许 LLM 或模板凭空编造依据。
- 所有质量判断必须来自 golden、snapshot、benchmark、CI、smoke 或真实外部连通证据。
- 所有外部资料和开源仓必须有 manifest、license/usage status、provenance 和导出边界。

## 3. 当前事实

基于当前 worktree 和 `governance/tasks/0010` 到 `0047`，本地已落地的基础设施切片如下：

| 域 | 已落地事实 | 仍不是生产 100% 的原因 |
| --- | --- | --- |
| 资源模型 | 已有 capability/provider/report/evaluation/observability/security/delivery 多类 schema 与 registry，并新增本地 OpenAPI 导出脚本 | 仍缺资源版本兼容策略、机器可读 lifecycle/promotion gate、正式 API changelog |
| API 发现 | 已有 `/metadata`、`/capabilities`、`/providers`、`/evaluations`、`/observability`、`/security`、`/surfaces` 等发现入口，并新增 developer sandbox fixture、curl/Python/Node/Agent 示例和 docs smoke | 当前只是本地开发者 baseline，仍缺公网 sandbox token 服务、固定输出 snapshot、发布版 SDK 包和外部开发者门户 |
| Job | 已有报告 job、幂等键、取消、TTL、状态查询、默认 memory / 可选 SQLite job store baseline、本地 webhook callback baseline、本地 restart-safe failure smoke、SQLite persistent webhook outbox record baseline、replayable report job recovery baseline、SQLite webhook outbox redelivery baseline、SQLite encrypted webhook delivery config vault baseline、SQLite webhook outbox lease claim/release baseline，并新增 Postgres ReportJobStore adapter、migration/job live smoke baseline、Postgres outbox worker lease negative smoke、Postgres job worker lease primitive smoke、Postgres external worker restart smoke、Postgres public webhook live smoke gate 与 Postgres worker heartbeat/polling smoke baseline | SQLite 只覆盖单副本本地持久状态；Postgres live smoke 只证明 schema/job/event/outbox/config 真实数据库路径，Postgres outbox lease smoke 只证明 webhook outbox duplicate claim 负例，Postgres job worker lease smoke 只证明 job claim primitive，Postgres external worker restart smoke 只证明过期 lease 的 stale running job 可恢复且双 manager 只有一个执行，Postgres public webhook live smoke gate 只证明外部 live 执行入口已建立，Postgres worker heartbeat/polling smoke 只证明短 lease heartbeat、persisted queued job polling 和 expired running job polling recovery。仍不覆盖 exactly-once、真实公网 webhook live passed evidence、外部 Vault/KMS、长期多副本运行或生产密钥生命周期 |
| Provider | bazi/ziwei/almanac/meihua 已接入统一 provider 协议 | 仍缺 provider 级 trace、外部依赖探测、版本锁、license gate、capability promotion gate |
| Report/Evidence | 已有 Report envelope、policyGate、snapshotGate、evidenceRefs schema，并新增八字/紫微 L4 golden evidence 本地 smoke baseline | 仍缺全文 golden diff、大规模真实匿名命例 coverage report、人工审核后台 |
| Evaluation | 已有 Dataset/EvaluationRun registry、runner、history、diff policy，八字/紫微核心样板已具备 quick/full 本地 golden smoke 入口，并新增本地 dashboard/nightly artifact baseline | 仍缺长期结果库、扩展 corpus、远端 CI 对当前 diff 的证据和独立评测服务 |
| Observability | 已有 health/ready/metrics/requestId/structured log、本地 smoke，并新增 W3C traceparent/OpenTelemetry 语义兼容 span 日志、SLO policy 和 alert rules 本地 baseline | 仍缺 OpenTelemetry SDK exporter/collector、trace backend、真实生产流量 error budget、外部告警平台和生产监控平台 |
| Security/Privacy | 已有 SecurityControl、security smoke、secret scan、audit_event、retention baseline、scoped RBAC baseline，并新增生产身份/OIDC、SIEM/不可变审计存储、retention 自动清理计划、SQLite records/report jobs cleanup smoke 和 OWASP API regression 的本地准入 contract/gate baseline | 仍缺真实 OAuth/OIDC、外部 IdP、多租户 IAM、外部 SIEM、不可变审计存储、生产 scheduler、Postgres production cleanup live、真实 live smoke |
| Delivery | 已有 DeliverySurface registry、同源链路声明，并新增 ReleaseGate registry/contract baseline | 仍缺 Web/API/Bot/CLI/Skill 全链路 live、字节级同源报告 diff、SDK 级接入验证 |
| 生产发布 | 已有 production-readiness 脚本、public-release gate、live release evidence gate JSON baseline、本地 SBOM/provenance、本地 rollback drill、本地 container release evidence，并已用公开 HF Space/API 通过 `production_api_live` 与 `hf_space_live`；0046 已完成 clean commit/push，Container workflow `28575853017` 与 Acceptance workflow `28575852876` 均对 `2b3f4c8` 通过 | 仍缺真实 Telegram Bot live、真实 registry digest/signature、远端 CI attestation、私有生产域名/token/CORS 实测 |

当前 worktree 在本轮刷新开始时为 clean；所有“已落地”仅代表仓库/本地/远端已注明的证据范围，不自动等于外部生产平台已验证。

## 4. 100% 验收域

### D0 开发者接入面

目标：外部开发者不读源码也能接入。

必须完成：

- OpenAPI：覆盖 metadata、capability、job、report、provider、evaluation、security、observability、delivery。本地导出与必备路径 smoke 已作为 0029 baseline 落地。
- 标准错误码：HTTP status、error code、retryable、userAction、debugAction。
- Sandbox：固定 token、固定样例、固定输出 snapshot。0029 只完成本地固定样例；固定 token 与输出 snapshot 仍是后续任务。
- SDK examples：curl、Python、Node、Agent tool call。0029 已完成最小示例和 docs smoke，不等于发布版 SDK。
- API changelog：版本、兼容、弃用、迁移。

完成证据：

- `openapi.json` 或等价导出文件。
- API contract tests。
- docs curl smoke 可执行。
- SDK 示例进入 quick CI 或 docs smoke。

### D1 Capability 控制面

目标：能力统一发现、准入、执行和拒绝。

必须完成：

- Capability registry 成为唯一真相源。
- planned 能力必拒绝执行。
- production 能力必须声明 provider、schema、risk、testGate、evidencePolicy。
- admission gate 在执行前统一检查状态、输入、风险、profile、可见性。
- capability promotion 从 L0 到 L4 有 checklist 和审计证据。

完成证据：

- registry schema 严格测试。
- planned execution negative tests。
- production capability gate tests。
- promotion checklist 文档和 closeout。

### D2 Job 与持久运行面

目标：长报告和批量评测可恢复、可取消、可追踪。

必须完成：

- Job store 抽象：memory / sqlite / external。当前已完成 memory / sqlite baseline、Postgres adapter baseline、Postgres migration/job live smoke baseline、Postgres outbox worker lease negative smoke、Postgres job worker lease primitive smoke、Postgres external worker restart smoke、Postgres public webhook live smoke gate、Postgres worker heartbeat/polling smoke baseline、外部 secret provider evidence gate baseline 和长期多副本 runtime evidence gate baseline；生产级 external runtime 仍缺 exactly-once、外部 Vault/KMS live passed、公网 webhook live passed evidence 和长期多副本运行 live evidence。
- 跨进程/跨重启幂等。当前 0055 已证明 SQLite manager 重建后旧任务安全失败且幂等键保留；0057 已证明带 `task_payload` 和 factory 的 Web/Markdown 报告任务可在本地 manager 重建后重新入队执行；0060 已证明 SQLite webhook outbox 本地 lease claim/release 可避免同一 failed outbox 在本地重复 claim；0071 已证明 Postgres schema/job/event/outbox/config 真实数据库路径；0072 已证明 Postgres webhook outbox duplicate claim 负例、错误 owner release 无效与 lease expiry reclaim；0074 已证明 Postgres job worker duplicate claim 负例、错误 owner release 无效、lease expiry reclaim 和 terminal job unclaimable；0075 已证明 stale running job 在过期 lease 后可由重启 manager 恢复执行且双 manager 只有一个执行；0076 已证明公网 webhook live smoke gate 已建立但未提供本地真实 endpoint passed evidence；0078 已证明短 lease heartbeat、persisted queued job polling 和 expired running job polling recovery；0080 已证明长期多副本 evidence schema 和反伪造负例；exactly-once、公网 webhook live passed evidence 和长期多副本运行 live evidence 仍未实现。
- job state transition guard。
- retry / timeout / non-retryable error policy。当前 0053 已完成本地 baseline；生产硬 timeout 与 external backend 仍待后续。
- webhook callback contract、签名、模拟器、本地有限 retry 与 outbox trail。当前已完成本地 callback baseline、SQLite persistent outbox record baseline、SQLite resolver redelivery baseline、SQLite encrypted config vault baseline、SQLite outbox lease claim/release baseline 和外部 secret provider evidence gate baseline；真实公网 live、外部 Vault/KMS live passed、生产密钥生命周期与生产分布式交付仍未实现。
- job event history。当前 0052 已完成 memory/sqlite event history 首切片，0055 已覆盖 `job.recovered_failed` 本地 smoke，0056 已覆盖 webhook outbox record，0057 已覆盖 `job.recovered_requeued` 本地 smoke，0058 已覆盖 webhook redelivery 事件，0059 已覆盖 encrypted config vault 重投恢复，0060 已覆盖本地 outbox lease 互斥与重投恢复，0071 已覆盖 Postgres event idempotency 与 outbox claim/release live smoke，0072 已覆盖 Postgres outbox worker lease negative smoke；后续仍需与 job execution worker、live webhook 和外部 secret storage 统一。

完成证据：

- SQLite backend 回归、job event history 首切片、本地 retry/timeout policy 首切片、本地 callback retry/outbox trail、本地 restart-safe failure smoke、SQLite persistent webhook outbox record baseline、replayable report job recovery baseline、SQLite webhook outbox redelivery baseline、SQLite encrypted webhook delivery config vault baseline、SQLite webhook outbox lease claim/release baseline、Postgres adapter、Postgres migration/job live smoke、Postgres outbox worker lease negative smoke、Postgres job worker lease primitive smoke、Postgres external worker restart smoke、Postgres public webhook live smoke gate、Postgres worker heartbeat/polling smoke baseline、外部 secret provider evidence gate、长期多副本 runtime evidence gate 和 webhook simulator 已完成；真实 webhook live passed evidence、生产硬 timeout、外部 Vault/KMS live passed、生产密钥生命周期、长期多副本运行 live evidence 和 exactly-once 仍待后续。
- 幂等参数不一致 negative test。
- webhook local simulator。
- crash/restart smoke 或等价持久化恢复测试。当前已有本地 restart-safe failure smoke 和 replayable recovery smoke；生产级外部 backend 恢复仍待后续。

### D3 Provider 生命周期

目标：每个测算体系像基础设施 provider 一样被管理。

必须完成：

- Provider metadata：engineVersion、source、licenseStatus、deterministic、health、dependencies。当前已完成本地 lifecycle metadata baseline。
- Provider health：本地和外部依赖两层。当前已有进程内 health 与本地 dependency fixture smoke；真实公网外部依赖 live smoke 仍待后续。
- Provider error normalization。
- Provider version lock。当前已完成本地 versionLock gate。
- Provider trace span。
- Provider resource manifest。当前已完成 runtime/contract/test/supply-chain refs baseline。
- Provider deprecation / replacement policy。当前已完成 active/deprecated 策略字段和 gate。

完成证据：

- `/providers/{id}` 暴露完整 metadata。
- provider lifecycle gate：`bash scripts/provider-lifecycle-gate.sh`。
- provider dependency smoke：`bash scripts/provider-dependency-smoke.sh`。
- provider exception tests。
- dependency/license/source manifest 校验。

### D4 Report 与 Evidence

目标：输出自然，但证据机器可读。

必须完成：

- Report schema 覆盖 JSON/Markdown/sections/evidenceRefs/risk/policyGate/snapshotGate。
- Markdown 全文 golden diff。
- Evidence coverage report。
- 规则冲突裁决器。
- forbidden claims scanner。
- report profile registry。
- 人工审核后台或人工审核导出包。

完成证据：

- 默认综合八字报告 snapshot。
- 紫微独立 profile snapshot。
- 八字/紫微 L4 golden evidence smoke：`bash scripts/bazi-ziwei-l4-golden-smoke.sh --profile quick`。
- evidenceRefs 不断链测试。
- forbidden claims negative cases。

### D5 数据、典籍与供应链

目标：资料和外部仓库可追溯、可分级、可导出。

必须完成：

- Dataset/DataSupplyChain manifest：raw/canonical/derived/reference/runtime/export（0035 已完成本地 registry/schema baseline）。
- Classics manifest：书名、作者、来源、许可/可用性、canonical path、引用规则（0035 已补 canonical TXT manifest/copyright coverage gate）。
- Vendor manifest：GitHub URL、commit、license、用途、production eligibility（已有 `vendor_sources.json`，0035 已接入 data supply chain gate）。
- Solar terms golden provenance：原始 hash、生成脚本、fixture（0035 已校验 source manifest hash 字段，外部 raw 授权仍待人工复核）。
- SBOM / artifact provenance（本地 release artifacts baseline 已完成；远端 CI attestation、registry signature 和容器 digest 仍待后续）。
- export hygiene：raw/vendor/cache/runtime 默认排除。

完成证据：

- manifest schema 与校验脚本：`contracts/fate/data-supply-chain/registry.json`、`contracts/fate/data-supply-chain/schemas/data-supply-chain.schema.json`、`bash scripts/data-supply-chain-gate.sh`。
- source/vendor/license gate：`bash scripts/data-supply-chain-gate.sh`、`bash scripts/vendor-health.sh`。
- export hygiene gate。
- 原始表 hash 校验。

### D6 评测与质量平台

目标：任何升级都能证明没有退化。

必须完成：

- 八字 golden：节气、真太阳时、起运、格局、用神、岁运、报告结构。
- 紫微 golden：十二宫、星曜、四化、大限流年、典型格局。
- MingLi-Bench runner 接入为可选/扩展 gate。
- Evaluation history 长期存储。
- Diff dashboard 或 HTML report。0036 已完成本地静态 HTML dashboard renderer 和 dry-run dashboard smoke。
- Threshold policy：blocking/warning/info。
- Nightly eval。0036 已完成本地 nightly wrapper 与 GitHub scheduled workflow artifact baseline；远端当前 diff 证据仍需 push 后由 Actions 产出。

完成证据：

- `scripts/run-evaluations.sh --all-local-required`。
- `scripts/compare-evaluations.sh`。
- `scripts/evaluation-dashboard.sh --summary-json <summary> --output-html <dashboard>`。
- `scripts/evaluation-nightly.sh --output-dir <dir>`。
- dashboard/report artifact。
- CI/nightly 链接；当前未 push diff 时只能记录为“远端 CI 待执行”。

### D7 SRE 与可观测性

目标：生产问题可定位、可衡量、可回滚。

必须完成：

- OpenTelemetry traces：API、job、provider、report。0037 已完成 W3C `traceparent` 与 OpenTelemetry 语义兼容的本地 span 日志 baseline；SDK exporter、collector、trace backend 仍待后续。
- Metrics：请求、错误、延迟、队列、provider failure、policy gate failure、eval failure。
- Logs：结构化、脱敏、requestId/traceId。0037 已让 HTTP request log 带 `traceId`。
- SLO：availability、latency、error rate、job success、provider success。0037 已完成本地 SLO policy baseline；真实 error budget 计算依赖生产监控平台。
- Alert：queue depth、error budget burn、provider outage、secret scan failure。0037 已完成本地 alert rules baseline；外部告警平台仍待后续。
- Runbook：故障、回滚、降级、禁用 provider。

完成证据：

- local OTel-compatible smoke：`bash scripts/observability-trace-slo-smoke.sh`。
- Prometheus metrics tests。
- SLO 文档和 alert rules：`contracts/fate/observability/slo-policy.json`、`contracts/fate/observability/alert-rules.json`、`bash scripts/observability-slo-gate.sh`。
- 生产监控平台截图或链接。

### D8 安全、隐私与合规边界

目标：公开环境默认安全，不泄露用户隐私。

必须完成：

- RBAC baseline：admin/user/owner/scopes。
- OAuth/OIDC 或生产级身份系统接入。0038 已完成本地准入 contract；0065 已补 externalization evidence contract 和本地负向伪造 gate；真实 OIDC/IdP 仍待外部环境实现与验证。
- Secret scan：first-party、docs、tests、tracked/untracked 非忽略文件。
- Audit log：脱敏、不可变或外部 SIEM。0038 已完成外部 SIEM/不可变审计存储准入 contract；0065 已补 live evidence 必备字段和 placeholder SIEM 负测；真实平台连通仍待执行。
- Retention：record、job、audit、log、export。0038 已完成 retention 自动清理计划 contract；0065 已补 retention cleaner smoke evidence 契约和缺 smoke 负测；0091 已完成本地 SQLite records/report jobs 按年龄清理 baseline；生产 scheduler、Postgres production cleanup live 和外部 SIEM/log retention evidence 仍待外部验证。
- Privacy regression：示例白名单、报告脱敏、地区展示边界。
- OWASP API 风险回归：BOLA、认证、资源消耗、配置、库存。0038 已完成 OWASP API Security Top 10 2023 本地映射 gate baseline。

完成证据：

- RBAC negative tests。
- OIDC live 或 staging evidence；当前已有本地 contract/gate 和 0065 externalization evidence negative gate。
- SIEM/log retention evidence；当前已有本地 contract/gate 和 0065 externalization evidence negative gate。
- privacy/security smoke。
- production readiness live smoke。

### D9 多端交付

目标：Web/API/Bot/CLI/Skill 同源输出。

必须完成：

- Web profile switch 只切独立体系，不污染默认综合八字。
- API canonical endpoint。
- Bot capability selection 和队列。
- CLI capability command。
- Skill install/run/export e2e。
- Markdown copy contract。
- 多端 byte-level 或 semantic diff。

完成证据：

- Web/API/Bot/CLI/Skill e2e。
- markdown snapshot diff。
- Hugging Face Space live smoke。
- Telegram Bot live smoke。

### D10 生产发布与运营

目标：能被第三方审计、应用开发者接入、真实生产运行。

必须完成：

- Container image build/release/smoke。
- GitHub Actions 当前 commit 通过。
- Release provenance/SBOM。本地 baseline 已由 `scripts/release-artifacts.sh` 生成；远端 attestation 仍待外部 CI/registry。
- Production readiness：真实 API 域名、CORS、token、Bot、TLS、HSTS、rate limit。
- Rollback protocol。
- Public status/health。
- Incident response runbook。

完成证据：

- CI URL。
- image digest。
- SBOM/provenance artifact。本地生成可用；远端 CI artifact/attestation 仍待执行。
- production-readiness output。
- `bash scripts/live-release-gate.sh --require-live ...` 输出的 release evidence JSON。
- live smoke output。
- release notes。

## 5. 剩余任务树

```text
MI-100 FateCat 测算基础设施 100%
  MI-00 当前切片收口
    MI-00.01 清理当前 worktree 任务证据、提交、推送、跑远端 CI（0046 已完成 clean/push；Container 与 Acceptance 均对 `2b3f4c8` 通过）
    MI-00.02 对 0010-0046 做审计交接，确认哪些只在本地、哪些已远端验证
    MI-00.03 0047 post-0046 计划刷新：把 0009-0046 事实、官方调研和 MI-NEXT 任务树落盘
  MI-01 开发者平台
    MI-01.01 OpenAPI 导出和 schema 分组（0029 已完成本地导出；schema 分组仍待做）
    MI-01.02 SDK/curl/Agent 示例和 docs smoke（0029 已完成最小本地 baseline；发布版 SDK 仍待做）
    MI-01.03 Sandbox token、sandbox fixture、固定输出（0029 已完成 fixture；0086 已完成固定输出 snapshot；0087 已完成本地 sandbox access gateway baseline；公网 token issuer/revocation/live gateway 仍待做）
    MI-01.04 API changelog 和兼容策略
  MI-02 控制面
    MI-02.01 Capability admission gate
    MI-02.02 L0-L4 promotion checklist
    MI-02.03 planned/experimental/production negative tests
    MI-02.04 registry lifecycle/status drift scanner
  MI-03 持久运行面
    MI-03.01 Job store 抽象（0030 已完成 memory/sqlite baseline；external 仍待做）
    MI-03.02 SQLite job backend（0030 已完成单副本本地持久化；多副本锁不在本轮）
    MI-03.03 webhook callback contract 和签名（0031 已完成本地 baseline；真实公网连通与重试仍待做）
    MI-03.04 retry/timeout/non-retryable policy
    MI-03.05 restart recovery smoke
  MI-04 Provider 生命周期
    MI-04.01 provider version lock（0032 已完成本地 baseline）
    MI-04.02 provider source/license/resource manifest（0032 已完成本地 baseline；人工法律复核仍待做）
    MI-04.03 provider health 和本地 dependency smoke（0033 已完成本地 baseline；真实公网 live 仍待做）
    MI-04.04 provider trace span
    MI-04.05 provider deprecation policy
  MI-05 八字/紫微 L4 样板
    MI-05.01 八字节气/真太阳时/起运外部 golden 加硬（0034 已完成本地代表样本 smoke；外部真实 corpus 仍待扩展）
    MI-05.02 八字格局/用神/调候/evidence coverage（0034 已完成本地规则深度代表样本 smoke；大规模命例仍待扩展）
    MI-05.03 紫微十二宫/星曜/四化/运限 golden（0034 已完成本地代表样本 smoke；更多格局盘仍待扩展）
    MI-05.04 八字/紫微报告 profile snapshot（0034 已完成 Markdown policy/snapshot gate smoke；全文 golden diff 仍待做）
    MI-05.05 冲突解释和反证说明（0034 已完成代表样本字段 gate；自动裁决器仍待升级）
  MI-06 数据与供应链
    MI-06.01 Dataset/Classics/Vendor manifest schema（0035 已完成 data supply chain registry/schema baseline）
    MI-06.02 raw/canonical/derived 数据分层 gate（0035 已完成本地 data-supply-chain-gate baseline）
    MI-06.03 许可证/版权/生产可用性分级（0035 已完成机器可读分级；人工法律意见仍待后续）
    MI-06.04 SBOM/provenance artifact（0040 已完成本地 baseline；远端 attestation/registry signature 待后续）
    MI-06.05 export hygiene 扩展到新 manifest
  MI-07 评测平台
    MI-07.01 golden corpus 扩容
    MI-07.02 MingLi-Bench runner 接入
    MI-07.03 evaluation dashboard 或 HTML report
    MI-07.04 nightly eval 和远端 CI artifact
    MI-07.05 diff policy 阈值分级
  MI-08 SRE/可观测
    MI-08.01 OpenTelemetry SDK/collector（0037 已完成本地 W3C traceparent/OTel 语义兼容 baseline；SDK/collector 待后续）
    MI-08.02 API/job/provider/report trace（0037 已完成 API/provider/report 本地 spans；跨线程 durable job trace 待后续）
    MI-08.03 SLO/error budget（0037 已完成本地 policy；真实生产 error budget 待后续）
    MI-08.04 alert rules（0037 已完成本地 rules；外部告警平台待后续）
    MI-08.05 runbook 和 incident response
  MI-09 安全/隐私
    MI-09.01 scoped RBAC baseline
    MI-09.02 OAuth/OIDC 或生产身份系统（0038 已完成本地准入 contract；0065 已完成 evidence contract/negative gate；真实 OIDC/IdP 待后续）
    MI-09.03 external SIEM/不可变审计存储（0038 已完成本地 contract；0065 已完成 evidence contract/negative gate；真实平台连通待后续）
    MI-09.04 retention 自动清理（0038 已完成计划 contract；0065 已完成 evidence contract/negative gate；0091 已完成本地 SQLite records/report jobs 清理 baseline；生产 scheduler、Postgres production cleanup live、外部 SIEM/log retention 仍待后续）
    MI-09.05 OWASP API security regression pack（0038 已完成本地 gate baseline）
  MI-10 多端交付
    MI-10.01 CLI capability command
    MI-10.02 Bot capability selection
    MI-10.03 Web/API/Bot/CLI/Skill semantic diff
    MI-10.04 Hugging Face Space live smoke
    MI-10.05 Telegram live smoke
  MI-11 生产发布
    MI-11.01 container release with digest
    MI-11.02 SBOM/provenance release artifact
    MI-11.03 production-readiness live gate
    MI-11.04 rollback drill
    MI-11.05 third-party audit package
```

## 6. Post-0046 下一批推荐任务

未来任务不再在路线图里强绑定具体目录编号；创建实际任务包时按当时 `governance/tasks/INDEX.md` 的下一个可用 ID 命名。

| 顺序 | 建议任务 | 为什么现在做 | 阻断/前置 |
| --- | --- | --- | --- |
| MI-NEXT-01 | Telegram Bot live smoke | 0048 已创建并执行；当前因缺真实 `FATE_BOT_TOKEN` 保持 Blocked | 真实 token，不输出 token；提供后重跑 `scripts/live-bot-smoke.sh` |
| MI-NEXT-02 | registry digest、signature、attestation | Done：0050 已补 GHCR digest、release artifact upload、GitHub artifact attestation 和 verify gate | 每个后续 release commit 仍需重跑 workflow |
| MI-NEXT-03 | durable runtime 二期 | SQLite/job/webhook baseline、job event history 首切片、本地 retry/timeout policy 首切片、本地 callback retry/outbox trail、本地 restart-safe failure smoke、SQLite persistent webhook outbox record baseline、replayable report job recovery baseline、SQLite webhook outbox redelivery baseline、SQLite encrypted webhook delivery config vault baseline、SQLite webhook outbox lease claim/release baseline、Postgres adapter、Postgres migration/job live smoke baseline、Postgres outbox worker lease negative smoke、Postgres job worker lease primitive smoke、Postgres external worker restart smoke、Postgres public webhook live smoke gate、Postgres worker heartbeat/polling smoke baseline、external secret provider evidence gate baseline 和 multi-replica runtime evidence gate baseline 已有，生产还需要真实公网 webhook live passed evidence、外部 Vault/KMS live passed、生产密钥生命周期、长期多副本运行 live evidence 和 exactly-once 边界 | 真实多副本环境、webhook 接收端、Vault/KMS 或 secret manager 权限 |
| MI-NEXT-04 | OpenTelemetry collector 与生产 SLO | 本地 trace/SLO/alert rules 已有，生产还需要 collector/exporter、backend、error budget 和告警证据 | 监控平台、告警平台权限 |
| MI-NEXT-05 | OIDC/SIEM/retention 实测 | 本地 contract/gate 已有，公开服务进入生产前需要真实身份、审计和数据保留闭环 | IdP/SIEM/存储权限 |
| MI-NEXT-06 | 八字/紫微 golden corpus 扩容 | 当前是 L4 代表样本，不足以对标市面强排盘系统的长期准确性 | 匿名样本、外部 golden、版权/隐私边界 |
| MI-NEXT-07 | MingLi-Bench 与外部 benchmark runner | 外部评测资产可作为独立质量信号，但必须先做 license/usage 分级 | 外部仓库许可和 fixture 清理 |
| MI-NEXT-08 | provider trace 与 lifecycle drift scanner | provider 已资源化，下一步要防止实际执行、依赖、license、manifest 漂移 | lifecycle gate 输出稳定 |
| MI-NEXT-09 | developer portal / SDK 发布 baseline | OpenAPI、示例、开发者平台 contract、developer portal、SDK release-readiness、固定输出 snapshot 和本地 sandbox access gateway baseline 已完成；开发者平台还差公网 developer portal、发布版 SDK、外部 sandbox token issuer/revocation 和 live gateway evidence | 发布渠道、sandbox secret、外部 issuer/gateway |
| MI-NEXT-10 | 第三方审计包 | 当 live gate 和外部证据闭合后，输出可交给审计方逐项复核的 handoff | MI-NEXT-01..09 证据齐备 |

## 6.1 2026-07-03 Post-0094 完整实现计划刷新

本次刷新基于当前 `main` 的 0093/0094 后状态。0093 已把 CLI capability command 变成可复核交付面；0094 已把 CLI/Skill 语义证据补进 multi-surface parity。后续不能再把“接口多了”误判为“基础设施完成”，应转入 4 条主线：核心测算质量、外部生产 live、开发者平台产品化、发布审计闭环。

### 6.1.1 Post-0094 当前判断

| 域 | 当前状态 | 100% 剩余缺口 | 下一步 |
| --- | --- | --- | --- |
| 多端交付 | Web/API/Bot dry-run/CLI/Skill 均已有本地同源或语义证据 | 真实 Bot/API/HF live 仍待执行 | 外部 live evidence pack |
| 核心质量 | L4 代表样本、core corpus baseline、MingLi-Bench aggregate gate 已有 | 八字/紫微更大匿名 corpus、全文 report diff、evidence coverage、冲突/反证自然语言化仍不足 | Wave A Next-01 |
| Durable runtime | Postgres adapter、worker lease、restart、heartbeat/polling、public webhook live gate 等基线已存在 | 长期多副本 live、生产 secret lifecycle、真实公网投递证据、exactly-once 边界仍未闭合 | Wave B Live-03/Live-06 |
| Event platform | CloudEvents/AsyncAPI 风格 contract baseline 已有 | producer/consumer 兼容、DLQ/replay 和负例仍需强化 | Wave A Next-02 |
| Security/privacy | RBAC、secret scan、retention cleanup baseline、OIDC/SIEM staged gate 已有 | 真实 IdP、SIEM、生产 retention scheduler、Postgres production cleanup live 仍待验证 | Wave A Next-03 + Wave B Live-05 |
| Observability/SRE | 本地 trace/SLO/alert rules 和 OTel staged gate 已有 | collector/backend、trace 查询、error budget、alert live、incident drill 仍待验证 | Wave B Live-04 |
| Developer platform | OpenAPI、示例、SDK release-readiness、developer portal contract、sandbox gateway baseline 已有 | 公网 portal、发布版 SDK/package、公网 sandbox issuer/revocation | Wave C |
| Release/audit | current release proof、audit bundle、rollback/artifact gate baseline 已有 | 每个 release commit 必须重跑并绑定当前 commit；第三方审计仍需 live evidence 闭合 | Wave D |

### 6.1.2 后续实现波次

| Wave | 顺序 | 任务 | 目标 | 证据 |
| --- | --- | --- | --- | --- |
| A | Next-01 | 八字/紫微 corpus/report diff expansion | 扩大核心质量回归集，不保存真实用户隐私或完整报告正文 | corpus manifest、coverage matrix、report diff policy、focused pytest、quick CI |
| A | Next-02 | Event platform consumer/replay contract tests | 让事件契约具备消费者兼容、replay 和 DLQ 证据 | producer/consumer tests、replay examples、negative cases |
| A | Next-03 | Retention production cleanup staged gate | 把本地 cleanup 推进到 Postgres/scheduler/SIEM 证据口径 | staged gate、blocked/live summary、anti-forgery tests |
| A | Next-04 | Provider/source/license long-running drift trend | 防止 provider/source/license 随时间漂移 | tracked baseline、trend summary、provider trace refs、license review pending list |
| A | Next-05 | 100% certification aggregator dry-run | 聚合 release、audit、external pending、core quality、security/SRE 为总门禁 | certification JSON，区分 passed/blocked/pending |
| B | Live-01 | Telegram Bot live smoke | 真实 Bot 连通 | live smoke output，不输出凭证 |
| B | Live-02 | Public API/HF live smoke | 真实公开 API 或 HF Space | TLS/CORS/token/response evidence |
| B | Live-03 | Public webhook live passed | 真实 Postgres + 公网 HTTPS receiver | signed event delivery、outbox terminal status、receiver proof ref |
| B | Live-04 | OTel backend/SLO/alert live | 真实监控平台 | trace query link、SLO dashboard、alert route evidence |
| B | Live-05 | OIDC/SIEM/retention live | 真实 IdP、SIEM 和 scheduler | issuer/JWKS/RBAC proof、SIEM query proof、retention run proof |
| B | Live-06 | Vault/KMS and multi-replica runtime live | 外部 secret manager + 多副本部署窗口 | key lifecycle、access audit、多副本 heartbeat/lease evidence |
| C | DX-01 | Public developer portal | 外部开发者不读源码即可接入 | portal smoke、OpenAPI artifact、examples links |
| C | DX-02 | SDK/package public release | 形成可安装 SDK 或官方 examples 包 | package publish evidence、install smoke |
| C | DX-03 | Sandbox token issuer/revocation | 提供受控体验环境 | issuer/revocation proof、scope/rate limit/audit tests |
| D | Ship-01 | Current release proof rerun | 每个 release commit 重新证明发布链路 | GitHub Actions URL、digest、attestation verify、rollback drill |
| D | Ship-02 | Current audit bundle rerun | 生成第三方可复核交接包 | audit markdown/json、risk register、pending external validations |
| D | Ship-03 | Third-party audit handoff | 让外部审计人员按证据复核 | 独立复核结果，未验证项不隐藏 |

### 6.1.3 当前最短路径

不等外部账号时，下一步优先执行 `Wave A Next-01：八字/紫微 corpus/report diff expansion`。原因：它不依赖生产凭证，却直接提高测算基础设施最核心的可信度。真实 Bot、公开 API/HF、OIDC、SIEM、OTel、Vault/KMS、多副本运行全部保留为 `外部连通验证待执行`，不能用 dry-run 或本地门禁替代。

### 6.1.4 0096 执行更新

0096 已将 `Wave A Next-01` 推进为本地可验证切片：紫微 basic corpus 从 4 个匿名北京样本扩到 8 个，新增覆盖标签和 `coverageRequirements`；`core-quality-corpus.json` 把 `corpus.ziwei.basic_cases.minCaseCount` 提升到 8；`report-diff-policy.json` 增加 `structuralDiff.summaryOnly`、必需结构摘要字段和完整报告正文禁止字段；`core-quality-corpus-gate.py` 会校验 coverage tags、summary-only 策略和 forbidden stored fields。该更新仍不保存完整报告正文，不引入真实用户样本，不声明八字/紫微专业能力 100%。

0097 已将 `Wave A Next-02` 推进为本地可验证切片：`events.json` 增加 producer/consumer compatibility、每事件 `consumerContract`、全局 replay policy 和 dead-letter policy；`examples/event-replay/` 增加脱敏 replay request 与 dead-letter record；`event-contract-gate.py` 会校验 producer path、required consumer、additive compatibility、replay source、DLQ 策略、负向用例和脱敏示例。该更新仍不接入真实 broker，不声明公网 webhook live delivery 或事件平台生产可用。

0098 已将 `Wave A Next-03` 推进为本地可验证切片：新增 `retention-production-cleanup-staged.json` 与 `retention-production-cleanup-gate.py`，聚合 production scheduler、Postgres cleanup 和 SIEM/log retention 的脱敏 staged evidence contract；默认输出 `shipGate=blocked` 与 `外部连通验证待执行`，并拒绝 raw URL、缺 Postgres smoke summary、`production_deleted=true`、真实 DSN/endpoint/token/secret 等伪证据。该更新仍不连接真实数据库、scheduler 或 SIEM，不执行生产删除，不声明 retention live passed；本地 closeout 证据为 `bash scripts/local-ci.sh --profile quick --output /tmp/fatecat-local-ci-0098-final-pass` 通过，focused regression `275 passed`。

0100 将 `Wave A Next-04` 推进为本地可验证切片：新增 `provider-drift-baseline.json`、`provider-drift-trend-contract.json` 与 `provider-drift-trend-gate.py`，把单次 provider drift scanner 的结果升级为 tracked baseline 对比。门禁会重新扫描 provider lifecycle、dependency smoke、trace span、source refs、license evidence 和 vendor metadata，并拒绝 provider 集合变化、source/license 指纹变化、vendor snapshot hash 漂移或 scanner finding 未清零。该更新仍不连接真实公网 provider，不做法律许可证结论，不声明外部依赖 live 或跨版本升级策略已完成。

0101 将 `Wave A Next-05` 推进为本地可验证切片：新增 `measurement-infrastructure-certification.json` 契约与 `measurement-infrastructure-certification.py` dry-run 聚合器，消费 local-ci 产物中的 provider、core quality、event、developer、security/privacy、observability/SRE、runtime、release 和 audit 证据，输出分域 `passed/blocked/pending/in-progress/failed` 与 `canClaim100Percent`。当前没有真实外部 live、release/audit 全闭合证据时，聚合器应输出 `blocked` 且 `canClaim100Percent=false`；该更新不替代第三方审计，不连接真实 Bot/API/HF/OIDC/SIEM/OTel/Vault/KMS，不声明 FateCat 已达到生产 100%。

0102 将 `Wave A A3` 推进为本地可验证切片：新增 `evidence-coverage-baseline.json`、`evidence-coverage-trend-contract.json` 与 `evidence-coverage-trend-gate.py`，把八字/紫微 evidence 从单次 broken-ref 检查升级为 tracked baseline 趋势门禁。门禁会聚合 `rule_depth_registry.json`、`classics_rule_index.json`、统一 `CapabilityExecutor` 输出和 capability Report `evidenceRefs`，拒绝规则引用断链、analysisEvidence trace 缺字段、Report evidenceRefs 完整度回退、冲突解释/反证字段缺失，以及 evidence items、appliedRules、conflicts、combinationStatements 低于 baseline。该更新不保存完整报告正文或真实用户资料，不证明预测准确率、专业能力 100%、外部 live 或第三方审计通过。

0103 将 `Wave A A4` 推进为本地可验证切片：`current-audit-bundle.py` 增加 `--local-ci-output-dir`，可从 local-ci output dir 读取 gate artifact 摘要，并把 `evidence-coverage-trend-gate.json` 作为 `evidence.evidence_coverage_trend_gate` 写入 current audit bundle 的 evidence index。local-ci 调用 current audit bundle 时会传入自身 output dir，因此最新审计包能直接追踪 0102 evidence coverage trend gate。该更新仍保持 local mode `auditGate=blocked`，不声明外部 live、required release proof 或第三方审计通过。

## 6.2 2026-07-03 Post-0098 深度调研与完整实现计划

本次刷新基于当前 `main` worktree、0096/0097 已完成事实、0098 本地 closeout 已通过事实，以及基础设施领域官方/事实标准资料。核心判断保持不变：FateCat 的 100% 不是预测准确率 100%，而是基础设施成熟度 100%。后续必须先把资源协议、控制面、证据门禁、生产连通和审计交付做成闭环，再扩展更多术数体系。

### 6.2.1 参考资料与同构映射

| 外部基础设施范式 | 参考资料 | FateCat 映射 |
| --- | --- | --- |
| 平台工程成熟度 | CNCF Platform Engineering Maturity Model: https://tag-app-delivery.cncf.io/whitepapers/platform-eng-maturity-model/ | capability、provider、job、report、audit、DX 作为平台能力治理。 |
| HTTP API 契约 | OpenAPI Specification: https://spec.openapis.org/oas/latest.html | API artifact、错误码、SDK 示例、兼容 changelog。 |
| 异步 API 契约 | AsyncAPI Specification: https://www.asyncapi.com/docs/reference/specification/latest | job/webhook/evaluation/release event 契约。 |
| 事件信封 | CloudEvents: https://cloudevents.io/ | 事件统一 envelope、DLQ、replay、审计。 |
| 控制面 | Kubernetes Controllers: https://kubernetes.io/docs/concepts/architecture/controller/ | spec/status、desired/current、drift scanner。 |
| 软件目录 | Backstage System Model: https://backstage.io/docs/features/software-catalog/system-model/ | Capability、Provider、Dataset、EvaluationRun、SecurityControl、DeliverySurface 可发现。 |
| Provider 生态 | Terraform Providers: https://developer.hashicorp.com/terraform/language/providers | provider version lock、source/license、compat matrix、deprecation。 |
| 持久工作流 | Temporal Durable Execution: https://docs.temporal.io/evaluate/understanding-temporal | CalculationJob event history、retry、restart recovery、external backend。 |
| 可观测 | OpenTelemetry Signals: https://opentelemetry.io/docs/concepts/signals/ | API -> job -> provider -> report traces/metrics/logs。 |
| SRE | Google SRE SLO: https://sre.google/sre-book/service-level-objectives/ | SLI/SLO/error budget、alert、runbook。 |
| API 安全 | OWASP API Security Top 10 2023: https://owasp.org/API-Security/editions/2023/en/0x11-t10/ | OIDC/RBAC、tenant isolation、rate/body limit、negative tests。 |
| 安全开发 | NIST SSDF SP 800-218: https://csrc.nist.gov/pubs/sp/800/218/final | secret scan、dependency review、release evidence、vulnerability response。 |
| AI 风险治理 | NIST AI RMF: https://www.nist.gov/itl/ai-risk-management-framework | AI/Agent 只解释结构化结果，禁止高风险替代建议和确定性恐吓断语。 |
| 供应链与 SBOM | SLSA v1.2: https://slsa.dev/spec/v1.2/；CycloneDX: https://cyclonedx.org/specification/overview/ | provenance、SBOM、attestation、release artifact。 |
| Artifact 证明 | GitHub Artifact Attestations: https://docs.github.com/en/actions/how-tos/secure-your-work/use-artifact-attestations/use-artifact-attestations | 每个 release commit 绑定 digest、attestation verify 和 CI URL。 |
| 幂等与 Webhook | [Stripe Idempotent Requests](https://docs.stripe.com/api/idempotent_requests)；[Stripe Webhooks](https://docs.stripe.com/webhooks) | report job idempotency、webhook signature、retry/outbox、event audit。 |

### 6.2.2 Post-0098 资源成熟度

| Resource | 当前状态 | 100% 目标 | 下一步 |
| --- | --- | --- | --- |
| Capability | registry/executor/profile 已有，planned 拒绝执行 | 全 production capability 统一 executor/policy/evidence/report profile | certification aggregator dry-run。 |
| Provider | provider protocol、dependency smoke、lifecycle/drift baseline 已有；0100 已新增 provider/source/license tracked baseline 与 drift trend gate | compat matrix、人审许可、真实公网外部依赖 live smoke | certification aggregator 后继续推进。 |
| CalculationJob | SQLite/Postgres/job/webhook/recovery 多个 baseline 已有 | 多副本长期运行、真实公网 webhook、external secret、exactly-once 边界 | external live Wave B。 |
| Event | 0097 已补 consumer/replay/DLQ contract baseline | 真实 broker/consumer 兼容、replay 操作审计、DLQ 处置闭环 | broker/consumer live evidence。 |
| ReportProfile | Markdown policy/snapshot、多端语义 diff 已有 | 全文结构 diff、人审抽样、profile 兼容策略 | report-diff 和 semantic hash 继续扩容。 |
| Evidence | evidenceRefs、规则索引、broken-ref gate 已有 | coverage 量化、冲突裁决、反证解释自然语言化 | evidence coverage trend。 |
| Dataset | data supply chain、core corpus baseline 已有 | 大规模匿名 corpus、历法/紫微/八字边界、人审分层 | corpus expansion + legal review。 |
| EvaluationRun | runner/history/dashboard/nightly baseline 已有 | current commit nightly artifact、外部 benchmark aggregate、趋势库 | quality trend store。 |
| DeliverySurface | Web/API/Bot/CLI/Skill registry 与本地 parity 已有 | 真实 Bot/API/HF live parity、SDK/package、developer portal | external live + DX waves。 |
| ObservabilitySignal | health/ready/metrics/logs/local spans/SLO rules/staged OTel 已有 | OTel collector/backend、SLO dashboard、alert live、incident drill | OTel backend live。 |
| SecurityControl | RBAC、secret scan、audit、retention、OWASP baseline；0098 staged cleanup 本地 closeout 已通过 | OIDC/IdP、SIEM、retention scheduler、tenant isolation live | OIDC/SIEM/retention external live。 |
| ReleaseArtifact | release artifact、digest/attestation baseline exists from earlier tasks | 每个 release commit 重新证明 digest/SBOM/provenance/rollback | current release proof rerun。 |
| AuditHandoff | audit generator/dry-run baseline 已有 | 第三方能逐项复核，pending external list 完整 | current audit bundle refresh。 |

### 6.2.3 完整执行波次

| Wave | 顺序 | 任务 | 目标 | 证据 |
| --- | --- | --- | --- | --- |
| 0 | 0.1 | Finish 0098 local closeout | 完成 retention staged gate final validation 和 task closeout | quick local-ci、secret scan、task validator |
| 0 | 0.2 | Current worktree hygiene | 对 0098/0099 做版本控制收口 | clean git status 或明确分批 commit plan |
| A | A1 | Provider/source/license long-running drift trend | 防止 provider/source/license 随时间漂移 | tracked baseline、trend summary、provider trace refs、license review pending list |
| A | A2 | 100% certification aggregator dry-run | 聚合 release、audit、external pending、core quality、security/SRE 为总门禁 | certification JSON，区分 passed/blocked/pending/in-progress |
| A | A3 | Evidence coverage trend | 把 broken-ref 从点状 gate 升级为覆盖率趋势 | coverage summary、counter-evidence cases、conflict explanation checks |
| A | A4 | Current audit bundle refresh | 基于最新 commits 重新聚合审计包 | audit markdown/json、risk register、external pending list |
| B | B1 | Telegram Bot live smoke | 真实 Bot 连通 | live smoke output，不输出凭证 |
| B | B2 | Public API/HF live smoke | 真实公开 API 或 HF Space | TLS/CORS/token/response evidence |
| B | B3 | Public webhook live passed | 真实 Postgres + 公网 HTTPS receiver | signed event delivery、receiver proof ref、outbox terminal state |
| B | B4 | OTel backend/SLO/alert live | 真实监控平台 | trace query link、SLO dashboard、alert route proof |
| B | B5 | OIDC/SIEM/retention live | 真实 IdP、SIEM 和 scheduler | issuer/JWKS/RBAC proof、SIEM query proof、retention run proof |
| B | B6 | Vault/KMS and multi-replica runtime live | 外部 secret manager + 多副本部署窗口 | key lifecycle、access audit、多副本 heartbeat/lease evidence |
| C | C1 | Public developer portal | 外部开发者不读源码即可接入 | portal smoke、OpenAPI artifact、examples links |
| C | C2 | SDK/package public release | 形成可安装 SDK 或官方 examples 包 | package publish/install smoke、version changelog |
| C | C3 | Sandbox token issuer/revocation | 提供受控体验环境 | issuer/revocation proof、scope/rate limit/audit tests |
| D | D1 | Current release proof rerun | 每个 release commit 重新证明发布链路 | GitHub Actions URL、digest、attestation verify、rollback drill |
| D | D2 | Third-party audit dry-run | 外部审计前自检证据完整性 | audit checklist、evidence index、unknowns list |
| D | D3 | Third-party audit handoff | 让审计人员逐项复核 | 独立复核结果，未验证项不隐藏 |

### 6.2.4 后续最短路径

1. 0101 `Wave A A2：100% certification aggregator dry-run` 已完成并推送。
2. 0102 `Wave A A3：Evidence coverage trend` 已落成本地 gate baseline；后续合法增强必须同步更新 baseline 和回归测试。
3. 0103 `Wave A A4：Current audit bundle refresh` 已落成本地 gate artifact evidence index 刷新；审计包能追踪 0102 evidence coverage trend gate。
4. 下一步进入 Wave B live tasks 或继续补质量趋势库：Bot/API/HF/webhook/OTel/OIDC/SIEM/Vault/KMS/多副本运行均需要真实外部凭证和平台。
5. DX 和 SDK 公开发布放在 Wave C，第三方审计交接放在 Wave D。

这些任务完成前，不能声明 FateCat 已经是 100% 测算基础设施；只能说“正在按 100% 基础设施路线推进，部分资源域已具备本地可验证 baseline，外部生产连通仍待执行”。

## 6.3 2026-07-03 Post-0103 深度调研与完整实现计划刷新

本次刷新基于当前 `main` 的 0100-0103 后状态、EvaluationRun runner/history/dashboard/nightly 已有事实，以及基础设施领域官方资料。结论保持收敛：FateCat 的 100% 不是“术数模块越多越好”，而是像支付、云原生、工作流、SRE、供应链和开发者平台一样，把测算能力做成可发现、可执行、可恢复、可观测、可审计、可发布、可交付的基础设施。

### 6.3.1 参考资料与同构要求

| 基础设施领域 | 参考资料 | FateCat 必须学习的能力 |
| --- | --- | --- |
| 平台工程 | CNCF Platform Engineering Maturity Model: https://tag-app-delivery.cncf.io/whitepapers/platform-eng-maturity-model/ | 平台作为产品，提供自助入口、能力目录、反馈闭环、度量和持续改进。 |
| HTTP API | OpenAPI Specification: https://spec.openapis.org/oas/latest.html | 不读源码即可接入 API，错误码、schema、示例、版本兼容可机器读取。 |
| 异步接口 | AsyncAPI Specification: https://www.asyncapi.com/docs/reference/specification/latest | job/webhook/evaluation/release 事件要有机器可读契约。 |
| 事件标准 | CloudEvents: https://cloudevents.io/ | 统一事件 envelope，支撑路由、重放、DLQ、审计和跨系统追踪。 |
| 控制面 | Kubernetes Controllers: https://kubernetes.io/docs/concepts/architecture/controller/ | 所有 capability/provider/release gate 都应有 desired/current/status 和 drift reconciliation。 |
| Provider 生态 | Terraform Providers: https://developer.hashicorp.com/terraform/language/providers | provider 独立版本、来源、许可、health、兼容矩阵、deprecation 和 rollback。 |
| 持久工作流 | Temporal Durable Execution: https://docs.temporal.io/evaluate/understanding-temporal | 长任务必须有 event history、retry、timeout、restart recovery 和外部 backend 证据。 |
| 可观测 | OpenTelemetry Signals: https://opentelemetry.io/docs/concepts/signals/ | API -> job -> provider -> report 全链路 traces/metrics/logs 可关联。 |
| SRE | Google SRE SLO: https://sre.google/sre-book/service-level-objectives/ | 用 SLI/SLO/error budget/alert/runbook 管生产可靠性。 |
| API 安全 | OWASP API Security Top 10 2023: https://owasp.org/API-Security/editions/2023/en/0x11-t10/ | 认证、授权、资源消耗、库存、配置、租户隔离和负例测试。 |
| 安全开发 | NIST SSDF SP 800-218: https://csrc.nist.gov/pubs/sp/800/218/final | secure defaults、secret scan、依赖审计、漏洞响应和交付证据。 |
| AI 风险 | NIST AI RMF: https://www.nist.gov/itl/ai-risk-management-framework | Agent 只解释结构化测算结果，禁止高风险替代建议和确定性恐吓断语。 |
| 供应链 | SLSA v1.2: https://slsa.dev/spec/v1.2/；CycloneDX: https://cyclonedx.org/specification/overview/ | SBOM、provenance、attestation、source/vendor/data manifest。 |
| 发布证明 | GitHub Artifact Attestations: https://docs.github.com/en/actions/how-tos/secure-your-work/use-artifact-attestations/use-artifact-attestations | 每个 release commit 绑定 digest、attestation verify、CI URL 和 rollback 证据。 |
| 产品质量趋势 | DORA Metrics: https://dora.dev/guides/dora-metrics-four-keys/ | 不能只看单次测试，要记录趋势、失败率、恢复时间和发布质量。 |

### 6.3.2 Post-0103 当前差距

| 域 | 已有基础 | 仍缺什么 | 0104/后续动作 |
| --- | --- | --- | --- |
| Capability/Provider | registry、executor、provider protocol、lifecycle/dependency/drift baseline | provider compat matrix、人审 license、真实外部依赖 live | 保持 A/B 质量门禁，后续做外部 live。 |
| EvaluationRun | runner、history/latest、diff、dashboard、nightly baseline | 长期趋势库、current commit 远端 artifact、外部 benchmark trend | 0104 新增本地 trend policy/gate/smoke。 |
| Core quality | 八字/紫微 L4、core corpus、evidence coverage trend | 更大匿名 corpus、全文结构 diff、人审抽样、冲突裁决自然语言化 | 后续扩样本和人审评估。 |
| Runtime | SQLite/Postgres/job/webhook/recovery/lease/heartbeat baseline | 真实公网 webhook、Vault/KMS、多副本长期运行、exactly-once 边界 | Wave B live。 |
| Observability | 本地 spans、SLO policy、alert rules、OTel staged gate | 真实 collector/backend、trace query、alert live、error budget | Wave B live。 |
| Security/Privacy | RBAC、secret scan、retention staged gate、OWASP baseline | OIDC/IdP、SIEM、生产 scheduler、tenant isolation live | Wave B live。 |
| Developer platform | OpenAPI、examples、SDK readiness、portal contract、sandbox gateway baseline | 公网 portal、SDK/package release、sandbox token issuer/revocation | Wave C。 |
| Release/Audit | release artifacts、current proof、audit bundle baseline | 每个 release commit 重跑、第三方审计人复核、外部 pending 全闭合 | Wave D。 |

### 6.3.3 100% 实现任务树

```text
MI-100 FateCat 测算基础设施 100%
  MI-100.01 Protocol and resource model
    MI-100.01.01 Capability / Provider / Dataset / EvaluationRun / DeliverySurface / SecurityControl schemas
    MI-100.01.02 Resource status, maturity, owner, lifecycle, version and policy fields
    MI-100.01.03 API / catalog / docs discovery parity
  MI-100.02 Execution control plane
    MI-100.02.01 Unified CapabilityExecutor for every production capability
    MI-100.02.02 planned/experimental refusal and negative gates
    MI-100.02.03 provider health, dependency, lifecycle, drift and deprecation
  MI-100.03 Durable calculation runtime
    MI-100.03.01 idempotency, cancellation, TTL, event history
    MI-100.03.02 retry/timeout/non-retryable policy
    MI-100.03.03 SQLite/Postgres/external backend and worker lease
    MI-100.03.04 public webhook live, outbox, signature, redelivery and replay
    MI-100.03.05 Vault/KMS and multi-replica live evidence
  MI-100.04 Evidence and report platform
    MI-100.04.01 ReportProfile snapshot/policy gates
    MI-100.04.02 evidenceRefs broken-ref fail-fast
    MI-100.04.03 evidence coverage trend and conflict/counter-evidence explanation
    MI-100.04.04 summary-only report diff and privacy regression
  MI-100.05 Evaluation platform
    MI-100.05.01 golden corpus and benchmark manifests
    MI-100.05.02 local runner, history/latest, diff, dashboard and nightly
    MI-100.05.03 trend policy/gate/store
    MI-100.05.04 remote current commit artifacts and external benchmark trend
  MI-100.06 Data and supply chain
    MI-100.06.01 source/vendor/data manifests, hash, license and usage role
    MI-100.06.02 SBOM/provenance/attestation and export hygiene
    MI-100.06.03 legal/manual review queue for non-code knowledge sources
  MI-100.07 SRE and observability
    MI-100.07.01 health/ready/metrics/logs/traces
    MI-100.07.02 OpenTelemetry collector/backend and trace query evidence
    MI-100.07.03 SLO/error budget/alert/runbook/incident drill
  MI-100.08 Security and privacy
    MI-100.08.01 OIDC/IdP, RBAC, tenant isolation and negative tests
    MI-100.08.02 SIEM/immutable audit storage and retention scheduler
    MI-100.08.03 secret management, privacy regression and AI risk policy
  MI-100.09 Developer and delivery surfaces
    MI-100.09.01 Web/API/Bot/CLI/Skill/HF same-profile semantic diff
    MI-100.09.02 OpenAPI/AsyncAPI/CloudEvents artifacts
    MI-100.09.03 SDK/package, developer portal, sandbox issuer/revocation
  MI-100.10 Release and audit
    MI-100.10.01 current release proof, digest, SBOM/provenance, attestation verify
    MI-100.10.02 rollback drill and live release gate
    MI-100.10.03 current audit bundle, audit dry-run and third-party handoff
```

### 6.3.4 Post-0103 执行顺序

| Priority | Task | Why | Evidence |
| --- | --- | --- | --- |
| P0 | 0104 Evaluation trend store | EvaluationRun 已有 history/diff/dashboard，但缺趋势门禁；这是质量基础设施的近因盲区 | `trend-policy.json`、`evaluation-trend-gate.py`、synthetic smoke、focused pytest、local-ci wiring |
| P0 | 0105 Current audit bundle evaluation trend evidence | 0104 已有本地趋势门禁，但 current audit bundle 必须能追踪该门禁，否则审计证据包会漏掉最新质量趋势证据 | `evidence.evaluation_trend_gate`、current audit bundle regression、local-ci current bundle artifact |
| P0 | Wave B live evidence | 当前最大缺口是外部生产连通，不是本地脚本 | Bot/API/HF/webhook/OTel/OIDC/SIEM/Vault/KMS 的真实 evidence |
| P0 | Core quality corpus expansion | 测算基础设施必须证明核心八字/紫微质量长期稳定 | corpus manifest、coverage matrix、summary-only report diff、人审抽样 |
| P1 | Remote evaluation artifacts | 本地趋势库之后要把远端 CI/current commit 和 external benchmark 接进趋势 | GitHub run URL、artifact digest、benchmark aggregate summary |
| P1 | Public DX release | 基础设施必须让开发者不读源码即可接入 | portal smoke、SDK install smoke、sandbox token issuer/revocation |
| P0 | Third-party audit package | 100% 声明必须能被外部审计复核 | audit handoff、risk register、pending external list、independent review result |

### 6.3.5 0104 执行更新

0104 将 EvaluationRun 的 `history/latest` 推进为本地可验证质量趋势门禁：新增 `trend-policy.json`、`evaluation-trend-gate.py`、shell wrapper、synthetic smoke、quick CI 接线、registry metadata 和 regression tests。门禁拒绝最新 summary 失败、连续失败、最新失败 run、失败命令和 required run 从历史中消失。输出保持 summary-only，不保存 stdout/stderr tail、benchmark 标准答案、完整报告正文、真实用户输入、token、secret 或 DSN。

0104 不能证明外部 benchmark、远端 CI current commit、生产 Bot/API/HF、第三方审计或预测准确率 100%；这些仍属于 `外部连通验证待执行` 或后续 Wave B/C/D。

### 6.3.6 0105 执行更新

0105 将 0104 的 EvaluationRun evaluation trend gate 纳入 current audit bundle：`current-audit-bundle.py` 会从 `--local-ci-output-dir` 读取 `evaluation-trend-gate-smoke/trend-gate.json`，验证 `kind=fatecat.evaluation_trend_gate`、`status=passed`、`trendFindings=[]`，并把 `evidence.evaluation_trend_gate` 写入 evidence index。这样第三方审计人员可以从 current audit bundle 追踪 0104 质量趋势门禁，而不是只在 local-ci summary 中看到 artifact 路径。

0105 仍然不声明远端 CI current commit、外部 benchmark、生产 API/HF/Bot live、第三方审计或 100% 基础设施完成；这些继续按不可伪造证据口径保留为后续 Wave B/C/D。

## 6.4 2026-07-04 Post-0105 深度调研与完整实现计划刷新

本次刷新基于当前 `main` worktree、0104 EvaluationRun trend gate、0105 current audit bundle evaluation trend evidence、以及当前 HEAD `e146d05` 无可见远端 GitHub Actions run 的事实。外部资料口径更新为：OpenAPI Specification latest 为 3.2.0、AsyncAPI latest 为 3.1.0、CycloneDX specification 已到 1.7；其余基础设施映射继续参考 CloudEvents、Kubernetes Controller、Backstage、Temporal、OpenTelemetry、DORA、SLSA 1.2、GitHub Artifact Attestations 和 Google SRE SLO。

### 6.4.1 Post-0105 事实

| 事实 | 证据 | 影响 |
| --- | --- | --- |
| 0104 已建立 EvaluationRun trend gate | `governance/tasks/0104-measurement-infrastructure-evaluation-trend-store/STATUS.md` | 质量评测不再只看单次 run。 |
| 0105 已让 current audit bundle 追踪 evaluation trend gate | `governance/tasks/0105-measurement-infrastructure-current-audit-bundle-evaluation-trend/STATUS.md` | 第三方审计包能看到最新质量趋势门禁。 |
| 当前 HEAD 无可见 Actions run | `gh run list --commit HEAD --limit 10 --json ...` 返回 `[]` | current release proof 不能声明远端 CI passed。 |
| workflow 是手动触发模型 | `.github/workflows/acceptance.yml`、`container.yml`、`hf-space-deploy.yml` 为 `workflow_dispatch`；`evaluation-nightly.yml` 另有 schedule | 缺远端 CI 不一定是配置错误，但它是 100% release proof 的硬缺口。 |

### 6.4.2 更新后的资源成熟度

| Resource | 当前状态 | 100% 目标 | 下一切片 |
| --- | --- | --- | --- |
| ReleaseArtifact | 本地 release proof、audit bundle 和 local-ci artifacts 已扩展；当前 HEAD 缺可见远端 Actions run。 | 每个 release commit 有 Acceptance/Container run URL、head SHA、digest、attestation verify、rollback drill。 | Current remote CI evidence refresh。 |
| EvaluationRun | runner/history/latest/diff/dashboard/nightly/trend gate/current audit bundle evidence 已有。 | 远端 current commit artifact、external benchmark aggregate、长期趋势和 failure taxonomy。 | Remote evaluation artifact integration。 |
| Evidence | evidence coverage trend 和 evaluation trend 已进入 audit bundle。 | 规则来源断链、覆盖率下降、冲突解释和反证字段回退全部 fail-fast。 | 继续扩 evidence conflict/counter-evidence gate。 |
| Core Bazi/Ziwei | L4 样本、core corpus、report diff、evidence coverage baseline 已有。 | 大规模匿名 golden、节气/起运/真太阳时边界、紫微典型格局、人审抽样。 | Core corpus deep expansion。 |
| Runtime | SQLite/Postgres/job/webhook/recovery/lease/heartbeat/Vault/KMS staged gate baseline 已有。 | 真实公网 webhook passed、外部 Vault/KMS live、多副本长期运行和 exactly-once 边界。 | External live runtime wave。 |
| ObservabilitySignal | local spans、OTel staged gate、SLO/alert baseline 已有。 | OTel collector/backend、trace query、SLO dashboard、alert route、incident drill。 | External OTel/SRE wave。 |
| SecurityControl | RBAC、secret scan、retention staged gate、OIDC/SIEM staged gate baseline 已有。 | 真实 IdP/SIEM/retention scheduler/tenant isolation live。 | External security live wave。 |
| DeliverySurface | Web/API/Bot/CLI/Skill/HF registry 与 semantic diff baseline 已有。 | 真实 Bot/API/HF live parity、SDK/package、developer portal、sandbox issuer/revocation。 | Public DX and live wave。 |
| AuditHandoff | current audit bundle 已能聚合多类 local-ci gate artifacts。 | 第三方审计可独立复核，所有 external pending 有 owner、证据路径和关闭条件。 | Third-party audit handoff wave。 |

### 6.4.3 下一批执行波次

| Wave | Priority | Task | 验收口径 |
| --- | --- | --- | --- |
| A1 | P0 | Current remote CI evidence refresh | 触发或捕获当前 commit Acceptance/Container run；只有 completed success 才能进入 current release proof；running、missing、failed 必须如实写入。 |
| A2 | P0 | Remote evaluation artifact integration | evaluation-nightly current commit artifact 能被 evaluation trend gate 和 current audit bundle 引用；不保存 benchmark 标准答案或报告正文。 |
| A3 | P0 | Core quality deep corpus expansion | 八字/紫微增加匿名边界样本、summary-only report diff 和 evidence trend；不使用真实用户样例。 |
| B1 | P0 | Production live proof pack | Bot/API/HF/public webhook/OIDC/SIEM/OTel/Vault/KMS/multi-replica live 逐项拿真实证据；没有凭证则写 `外部连通验证待执行`。 |
| C1 | P1 | Developer platform public release | developer portal、SDK/package、sandbox issuer/revocation、changelog 和 install smoke 可被外部开发者复核。 |
| D1 | P0 | Third-party audit handoff | 审计包能从 Git、CI、artifact、contract、registry、script 输出逐项复核；外部审计结果不得由本地 dry-run 替代。 |

### 6.4.4 0106 结论

0106 之后，最短路径不应继续堆新本地门禁，而应先补 `current remote CI evidence refresh`。原因是当前 local-ci、current audit bundle 和趋势门禁已经能证明本地质量链条，但 `gh run list --commit HEAD` 对当前提交没有可见 run；这会让 release proof 和最终 audit handoff 在“当前提交远端验证”处卡住。

只有以下证据全部闭合后，才能接近 100% 测算基础设施声明：

- 当前 commit 的远端 Acceptance/Container run URL、head SHA、success conclusion。
- 当前 release artifact 的 digest、SBOM/provenance、GitHub artifact attestation verify。
- 生产 API/HF/Bot/public webhook/OIDC/SIEM/OTel/Vault/KMS/multi-replica live evidence。
- 八字/紫微大规模匿名 golden、evidence coverage trend、EvaluationRun trend 和外部 benchmark aggregate。
- 第三方审计人员独立复核结果。

## 7. 100% 总验收清单

- [ ] 当前 worktree 所有基础设施切片完成提交、推送，并有远端 CI 对当前 commit 的通过证据。
- [ ] 所有资源 schema 完成并受 contract tests 保护。
- [ ] OpenAPI、错误码、SDK 示例、Sandbox 和 API docs smoke 完成。当前本地 OpenAPI 导出、最小示例、fixture、docs smoke、API changelog baseline、SDK/package baseline metadata、sandbox token contract、固定输出 snapshot 和本地 sandbox access gateway baseline 已完成；公网 sandbox token issuer/revocation/live gateway、外部开发者门户和发布版 SDK 仍未完成。
- [ ] 第三方审计 handoff 已可一键生成并可本地 dry-run 预检。当前 audit handoff Markdown/JSON generator、audit dry-run verifier、contracts 和 local-ci artifact baseline 已完成；外部审计人员独立复核和所有 live evidence 闭合仍未完成。
- [ ] 所有 production capability 有 provider interface、provider metadata、provider health、version lock 和 license/source status。
- [ ] planned 能力无法执行，experimental 能力默认不进入生产报告。
- [ ] 长任务支持幂等、取消、过期、持久 job store、event history、retry/timeout、webhook 和 restart recovery。当前已完成 memory/sqlite job store baseline、本地 webhook callback baseline、job event history 首切片、本地 retry/timeout policy 首切片、本地 callback retry/outbox trail、本地 restart-safe failure smoke、SQLite persistent webhook outbox record baseline、replayable report job recovery baseline、SQLite webhook outbox redelivery baseline、SQLite encrypted webhook delivery config vault baseline、SQLite webhook outbox lease claim/release baseline、Postgres adapter、Postgres migration/job live smoke baseline、Postgres outbox worker lease negative smoke、Postgres job worker lease primitive smoke、Postgres external worker restart smoke、Postgres public webhook live smoke gate、Postgres worker heartbeat/polling smoke、external secret provider evidence gate 和 multi-replica runtime evidence gate；真实 webhook live passed evidence、外部 Vault/KMS live passed、生产密钥生命周期、长期多副本运行 live evidence 和 exactly-once 仍未完成。
- [ ] 默认综合八字报告、紫微独立报告都有 Markdown/JSON snapshot、policy gate、evidence coverage。
- [ ] bazi/ziwei 达到 L4 checklist，边界 golden 和典型盘 golden 充足。
- [ ] 数据、典籍、vendor、benchmark 具备 manifest、来源、hash、license/usage status 和导出边界。
- [ ] evaluation runner、history、diff、dashboard、nightly 和远端 artifact 完成。
- [ ] OpenTelemetry traces、metrics、logs、SLO、alerts、runbook 完成。
- [ ] RBAC baseline、OAuth/OIDC、secret scan、audit、retention、privacy regression、OWASP API regression 完成。
- [ ] Web/API/Bot/CLI/Skill 同源输出有端到端和 live 证据。
- [ ] container release、SBOM/provenance、production-readiness、live bot smoke、rollback drill 完成。
- [ ] 第三方审计包能从 Git 状态、CI、命令、文档、registry、artifact 逐项复核。

## 8. 不可伪造证据口径

以下项目不能写“已完成”，除非有真实证据：

- 远端 CI：必须有 GitHub Actions run URL 和当前 commit。
- 生产 API：必须有真实域名、TLS、CORS、token、response evidence。
- Bot live：必须有真实 Bot token 和 dry-run 之外的 live smoke 证据。
- OIDC/SIEM/监控平台：必须有真实账号/环境的验证结果。
- SBOM/provenance：本地 baseline 必须有生成 artifact 和校验命令；生产发布还必须有远端 CI artifact/attestation。
- 典籍/外部资料生产可用性：必须有许可证/来源/使用边界复核。

没有这些证据时统一写：`外部连通验证待执行`。
