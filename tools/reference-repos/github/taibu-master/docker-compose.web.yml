services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: taibu-web:latest
    container_name: taibu-web
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${WEB_PORT:-3000}:3000"
