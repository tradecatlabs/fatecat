# Plan

# Planning Summary

Bridge the existing tracker issue evidence artifact chain into measurement infrastructure certification. This is the minimum local step after 0133: certification must consume every audit evidence chain local-ci already generates, without pretending external issue creation or live proof is complete.

# Lifecycle Gates

| Phase | Gate | Status |
| --- | --- | --- |
| SPEC | Scope excludes real issue creation and live proof | Done |
| PLAN | Contract/script/test/doc paths identified | Done |
| BUILD | Certification audit domain and contract updated | Done |
| TEST | Focused pytest, ruff, format, secret scan and quick local-ci | Done |
| REVIEW | Non-claim, privacy and doc drift reviewed | Done |
| SHIP | Commit/push/remote CI handled by outer delivery flow | Pending outside task docs |

不得跳过 gate。If real tracker credentials, issue permissions, live proof, certification or third-party audit evidence are required, the task must stop at `外部连通验证待执行` instead of fabricating issue creation or live validation evidence.

## SPEC

Certification audit domain must include tracker import package, tracker issue evidence template and tracker issue evidence gate as required evidence files.

## PLAN

1. Update `scripts/measurement-infrastructure-certification.py`.
2. Update `contracts/fate/audit/measurement-infrastructure-certification.json`.
3. Update `tests/regression/test_measurement_infrastructure_certification.py`.
4. Sync `scripts/AGENTS.md`, `contracts/fate/audit/AGENTS.md`, `tests/AGENTS.md`, roadmap and task index.
5. Run focused validation, secret scan, task docs and quick CI.
6. Commit, push and observe remote CI.

## BUILD

Implementation must reuse existing certification domain evaluation. No new script, storage, network client or issue tracker integration is introduced.

## TEST

Focused tests cover:

- contract required evidence files;
- blocked dry-run with tracker issue chain blockers;
- current audit bundle sidecar cannot bypass tracker issue blockers;
- synthetic full-pass still passes;
- require-certified still rejects blocked evidence.

## REVIEW

- Future-optimal drift: certification must consume all audit evidence chains generated by local-ci.
- Ponytail complexity: extend existing aggregator instead of adding a second certification layer.
- Document drift: AGENTS, roadmap and task index must mention this bridge.
- Security/privacy: aggregator only exposes artifact paths, status and blocking item labels.

## SHIP

This task remains a local certification bridge. It does not make real issue creation, live validation, independent audit or 100% certification complete.

# Simplest Path

Add three logical paths to the existing audit domain and three blocked markers. Treat `operator_action_required` as a blocked gate status.

# Split Strategy

The task is one vertical slice:

```text
local-ci tracker issue artifacts -> certification audit domain -> blocked certification summary
```

# Execution Waves

| Wave | Leaves | Result |
| --- | --- | --- |
| W1 | TP-01, TP-02 | Scope, contract and aggregator |
| W2 | TP-03, TP-04 | Regression and docs |
| W3 | TP-05 | Local validation |
| W4 | TP-06 | Git delivery and remote CI |

# Runtime Workflow Contract

Inputs:

- `--evidence-dir <local-ci-output-dir>`

Outputs:

- `fatecat.measurement_infrastructure_certification`

Side effects:

- Reads local JSON artifacts.
- Writes local certification JSON.
- Does not call network.
- Does not run `gh`.
- Does not create issues.

# Next Executable Leaves

- TP-05 validation gates.
- TP-06 git delivery and remote CI after local validation.

# Dependency Graph

```text
TP-01
  -> TP-02
    -> TP-03
      -> TP-04
        -> TP-05
          -> TP-06
```

# Rollback Protocol

Rollback is local and mechanical:

- remove tracker import/template/gate logical paths from `DOMAIN_SPECS`;
- remove `operator_action_required` from blocked gate handling if no longer needed;
- remove tracker required evidence fields from certification contract;
- revert regression and docs updates.

No database, external tracker, production endpoint or issue state is touched.

