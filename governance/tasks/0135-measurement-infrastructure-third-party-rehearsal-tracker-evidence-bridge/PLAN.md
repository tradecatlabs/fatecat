# Plan

# Planning Summary

Bridge the existing tracker issue evidence artifact chain into third-party audit rehearsal. This is the minimum local step after 0134: independent audit handoff must consume every audit evidence chain local-ci already generates, without pretending external issue creation or live proof is complete.

# Lifecycle Gates

| Phase | Gate | Status |
| --- | --- | --- |
| SPEC | Scope excludes real issue creation and live proof | Done |
| PLAN | Contract/script/test/doc paths identified | Done |
| BUILD | Rehearsal inputs, evidence index, checklist and local-ci updated | Done |
| TEST | Focused pytest, ruff, format, secret scan and quick local-ci | Done |
| REVIEW | Non-claim, privacy and doc drift reviewed | Done |
| SHIP | Commit/push/remote CI handled by outer delivery flow | Pending outside task docs |

不得跳过 gate。If real tracker credentials, issue permissions, live proof, certification or third-party audit evidence are required, the task must stop at `外部连通验证待执行` instead of fabricating issue creation or live validation evidence.

## SPEC

Third-party audit rehearsal must include tracker import package, tracker issue evidence template and tracker issue evidence gate as direct required evidence inputs.

## PLAN

1. Update `scripts/third-party-audit-rehearsal.py`.
2. Update `contracts/fate/audit/third-party-audit-rehearsal.json`.
3. Update `scripts/local-ci.sh`.
4. Update `tests/regression/test_third_party_audit_rehearsal.py`.
5. Sync `scripts/AGENTS.md`, `contracts/fate/audit/AGENTS.md`, `tests/AGENTS.md`, roadmap and task index.
6. Run focused validation, secret scan, task docs and quick CI.
7. Commit, push and observe remote CI.

## BUILD

Implementation reuses the existing rehearsal generator. No new script, storage, network client or issue tracker integration is introduced.

## TEST

Focused tests cover:

- contract required input files and expected kinds;
- build_report consumes tracker import/template/gate;
- evidence index includes three direct tracker artifact entries;
- auditor checklist includes three direct tracker checks;
- CLI requires and passes tracker inputs;
- raw URL/sensitive fragment rejection remains active.

## REVIEW

- Future-optimal drift: audit handoff must directly expose all local evidence chains generated by local-ci.
- Ponytail complexity: extend existing rehearsal generator instead of adding a second audit handoff layer.
- Document drift: AGENTS, roadmap and task index must mention this bridge.
- Security/privacy: rehearsal only exposes artifact paths, status, hash, gate and blocking item labels.

## SHIP

This task remains a local third-party audit rehearsal bridge. It does not make real issue creation, live validation, independent audit or 100% certification complete.

# Simplest Path

Add three required input paths to the existing rehearsal generator, append three evidence index entries, and add three checklist blockers. Keep all external/live blockers intact.

# Split Strategy

The task is one vertical slice:

```text
local-ci tracker issue artifacts -> third-party audit rehearsal -> blocked rehearsal checklist
```

# Execution Waves

| Wave | Leaves | Result |
| --- | --- | --- |
| W1 | TP-01, TP-02 | Scope, contract and CLI |
| W2 | TP-03, TP-04 | Evidence/checklist bridge and docs |
| W3 | TP-05 | Local validation |
| W4 | TP-06 | Git delivery and remote CI |

# Runtime Workflow Contract

Inputs:

- `--current-audit-bundle-json <path>`
- `--audit-dry-run-json <path>`
- `--current-release-proof-json <path>`
- `--certification-json <path>`
- `--closure-evidence-summary-json <path>`
- `--tracker-import-package-json <path>`
- `--tracker-issue-evidence-template-json <path>`
- `--tracker-issue-evidence-gate-json <path>`

Outputs:

- `fatecat.third_party_audit_rehearsal`

Side effects:

- Reads local JSON artifacts.
- Writes local rehearsal JSON and Markdown.
- Does not call network.
- Does not run `gh`.
- Does not create issues.

# Next Executable Leaves

- TP-06 git delivery and remote CI after local validation.

# Dependency Graph

```text
TP-01
  -> TP-02
    -> TP-03
      -> TP-04
        -> TP-05
          -> TP-06
```

# Rollback Protocol

Rollback is local and mechanical:

- remove tracker input arguments from `third-party-audit-rehearsal.py`;
- restore rehearsal contract required inputs and required kinds to the previous five-input shape;
- restore local-ci rehearsal invocation;
- revert regression and docs updates.

No database, external tracker, production endpoint or issue state is touched.
